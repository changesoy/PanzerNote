# A 路线 Handoff

## 定位

A 路线的目标是探索 PanzerNote 可以做到多轻。

它面向一个纯粹、可靠、快速的本地文本 / Markdown 编辑器，不要求承载完整 PanzerNote 的游戏系统，也不依赖 WebEngine 一类大型浏览器运行时。

可以将其理解为未来的 **PanzerNote Lite**。

## 核心思想

A 的价值不只是减小安装包，而是持续验证：

> 一个功能完整、长期可用的桌面编辑器，最低可以用多少运行时成本实现。

优先使用 Qt 原生能力和轻量依赖。对于 Markdown 图片、公式、图表等需求，可以尝试专用渲染器或其他体积较小的成熟组件，但不以“为了省空间而重新实现成熟标准”为目标。

重点应放在设计清晰的渲染边界，而不是自己重写 Mermaid、TeX、CSS 等已有生态。

## 游戏功能

Lite 发行版可以完全不包含游戏模块和游戏资源，使其保持纯编辑器定位。

但这不意味着 Editor / Core 应与完整 PanzerNote 分家。

长期更希望 A 与完整版共享核心代码，通过构建配置决定是否包含：

- 游戏系统；
- 游戏资源；
- Web Preview；
- 其他重量级可选能力。

避免形成两个长期独立维护的编辑器代码库。

## 当前方向

继续以当前 lightweight preview 为基础推进，优先关注：

- 编辑器本身的可靠性；
- Native Preview 的可用性；
- 启动速度与包体控制；
- Core / Editor 与可选功能之间的边界。

A 不需要证明“所有功能都能摆脱 Web 技术”。

它只需要回答：

> **如果用户只需要一个优秀的 PanzerNote 编辑器，我们能把它做到多轻？**
