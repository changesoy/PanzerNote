# PanzerNote 核心依赖
PyQt6>=6.11.0
shiboken6>=6.11.2  # PyQt6 绑定依赖，isValid 校验 C++ 对象存活
Pygments>=2.21.0
markdown>=3.10.3
Pillow>=12.3.0
send2trash>=2.1.0
markdown-it-py>=4.2.0
mdit-py-plugins>=0.6.1  # markdown 渲染扩展语法（定义列表/任务列表）

# C3-D：预览/PDF 导出改由系统共享的 WebView2 Runtime 承载，
# 不再随包分发 Qt WebEngine/Chromium（PyQt6-WebEngine 已摘除）。
# webview2-* 提供 webview2 命名空间；winrt-Windows.Foundation 提供
# winrt.windows.foundation；两者传递依赖 winrt-runtime。
# 注意：这三个包均为 Windows 专用（预览实现亦依赖标准库 winreg）。
qasync>=0.28.0  # WebView2 要求 asyncio 与 Qt 事件循环同线程合并
webview2-Microsoft.Web.WebView2.Core>=3.2.1
winrt-Windows.Foundation>=3.2.1

# 格式化支持（可选）
PyYAML>=6.0.3
tomli>=2.4.1
tomli-w>=1.2.0
cssbeautifier>=2.0.3
html5lib>=1.1

# 开发依赖
pytest>=9.1.1
pytest-cov>=7.1.0
pytest-qt>=4.5.0
pytest-timeout>=2.4
mypy>=2.3.1
