# PanzerNote v1.6.6 — 项目架构与设计文档

> 本文档旨在让任何人（包括新对话中的 AI）快速全面了解本项目的**目标、当前实现、代码结构、数据流、以及未完成规划**。

---

## 1. 项目背景

PanzerNote 是一款以已停服二次元游戏《战车少女》（PanzerMaiden）为主题的 **PC 端离线单机记事本程序**，用于私人纪念。核心理念：

- **记事本为主体**：功能完整的多标签文本编辑器，支持 Markdown 分屏预览与 30+ 语言语法高亮
- **游戏系统为灵魂**：通过日常书写行为积累资源 → 资源投入建造 → 抽取战车娘角色 → 点亮图鉴
- **小巧、离线、本地存档**：所有数据存储在本地 JSON 文件中，不依赖网络

**技术栈**：Python 3.11+ / PyQt6 / Pygments / markdown 库 / QWebEngine（可选）

---

## 2. 当前完成状态总览

| 模块                      | 状态      | 说明                                                                                                                   |
| ------------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------- |
| 多标签文本编辑器          | ✅ 完成   | 含行号、语法高亮、自动缩进、括号配对、行操作、大小写转换、转到行、JSON/XML格式化                                       |
| Markdown 分屏预览         | ✅ 完成   | 实时渲染 + 代码块语法高亮 + 一键复制 + 本地图片支持                                                                    |
| 代码缩略图 (Minimap)      | ✅ 完成   | 右侧鸟瞰图，彩色像素块，点击/拖拽导航，auto_minimap 支持                                                               |
| 增强型查找替换            | ✅ 完成   | 正则、大小写敏感、全词匹配、匹配计数、全部高亮                                                                         |
| 文件树                    | ✅ 完成   | 左侧文件树 + 外部文件区 + 右键菜单（新建/删除/重命名）+ 接受标签拖拽移动文件                                           |
| 标签页拖拽                | ✅ 完成   | 标签内排序 + 拖拽到文件树移动文件                                                                                      |
| 资源栏                    | ✅ 完成   | 顶部显示四资源 + 打字统计（UI 层面）                                                                                   |
| 在线/离线挂机             | ✅ 完成   | 在线每分钟 +5/+5/+5/铝材每3分钟+5；离线 1/3 向大取整，上限24h                                                          |
| 小秘书                    | ✅ 完成   | 右下角立绘 + 台词气泡 + 点击互动 + 事件台词系统 + 自定义角色/皮肤/状态                                                 |
| 设置系统                  | ✅ 完成   | settings.json + workspace.json + savegame.json，首次运行对话框选择路径                                                 |
| **安全防护体系**          | ✅ 完成   | 路径验证/文件操作安全/存档加密/输入验证/拖放白名单五模块，集成到核心文件操作流程                                       |
| **插件系统**              | ✅ 完成   | 插件生命周期管理/线程包装/12种权限控制/热加载，含2个示例插件和完整API文档                                              |
| **主题系统**              | ✅ 完成   | JSON/YAML外部主题加载/主题解析引擎/QSS样式生成/主题预览/ThemeAwareMixin全局生效                                        |
| **数据存储抽象层**        | ❌ 已删除 | 原IStorage接口/JSON存储适配器/SQLite存储适配器/存储工厂/数据迁移工具已移除（无业务调用方）                             |
| **打字奖励**              | ✅ 完成   | `textChanged` 信号接入 → 字符计数 → 递减收益算法 → 资源奖励（1:1:1:0.2 燃料/弹药/钢材/铝材）。v1.6.6 修复 IME 粘贴误判 |
| **每日签到**              | ✅ 完成   | 每日首次启动自动发放签到奖励（燃料/弹药/钢材/铝材各+100），通过小秘书提示                                              |
| **集中式版本管理**        | ✅ 完成   | `src/__init__.py` 为唯一真相源，`pyproject.toml` 动态引用，`verify_version.py` 一致性验证                              |
| **自动配对性能优化**      | ✅ 完成   | v1.6.6：frozenset O(1)快速过滤、\_doc_char_at单字符访问、\_wrap_selection选区包裹优化                                  |
| **MarkdownIt实例复用**    | ✅ 完成   | v1.6.6：\_create_md_parser() 创建一次并缓存，消除每次渲染重复实例化开销                                                |
| **文件保存异步化**        | ✅ 完成   | v1.6.6：SaveTask + QThreadPool 后台写入磁盘，UI 不再冻结                                                               |
| **Markdown预览增量更新**  | ✅ 完成   | v1.6.6：首次 setHtml 加载模板，后续 runJavaScript 仅更新 innerHTML                                                     |
| **Minimap块级增量失效**   | ✅ 完成   | v1.6.6：contentsChange 信号精确标记脏块，仅重绘受影响缓存块                                                            |
| **拖放文件类型白名单**    | ✅ 完成   | v1.6.6：仅允许文本文件类型拖入编辑器，非白名单扩展名弹出警告                                                           |
| **分屏语义优化**          | ✅ 完成   | v1.6.6：分屏打开新空白文件而非当前文件副本，菜单标注"独立编辑"                                                         |
| **封装违规修复**          | ✅ 完成   | v1.6.6：\_savegame_manager 改为 savegame_manager 公开属性                                                              |
| **行尾符统一**            | ✅ 完成   | v1.6.6：editor.py/menu_builder.py/plugin_manager.py CRLF→LF                                                            |
| **Silent except日志补全** | ✅ 完成   | v1.6.6：7个文件except...pass全部添加debug/warning日志                                                                  |
| **主题切换响应完善**      | ✅ 完成   | v1.6.6：\_game_placeholder/FindReplaceBar匹配标签随主题同步更新                                                        |
| **ErrorHandler正则收紧**  | ✅ 完成   | v1.6.6：File+line合并为单条正则，避免误过滤普通文案                                                                    |
| **建造系统**              | 🔲 规划中 | 四资源配方 + 等待时间 + 阈值过滤 + 概率抽取（已有 characters.json 数据）                                               |
| **图鉴系统**              | 🔲 规划中 | 已获得/未获得 + 立绘查看 + 语音播放（已有 savegame 中 `owned_characters`）                                             |
| **车库系统**              | 🔲 规划中 | 角色详情界面                                                                                                           |
| **游戏设置界面**          | 🔲 规划中 | 占位 `_show_game_settings` 空函数                                                                                      |

---

## 3. 目录结构

```
PanzerNote/
├── main.py                         # 程序入口（高DPI、字体、图标、首次运行引导、启动分析、版本一致性检查）
├── requirements.txt                # PyQt6, PyQt6-WebEngine, Pygments, markdown, Pillow, cryptography, send2trash, markdown-it-py
├── pyproject.toml                  # 项目配置（pytest/mypy/依赖/动态版本引用）
├── .gitignore                      # Git 忽略规则（__pycache__/data/config/data/logs/等）
├── .gitattributes                  # Git 属性（* text=auto eol=lf 行尾符统一）
├── user_data_path.txt              # 持久化用户数据路径
│
├── tests/                          # ══════ 单元测试 ══════
│   ├── benchmarks/                 # 性能基准测试（测试数据生成器/运行器/基线入口/结果）
│   ├── test_logger.py              # 结构化日志系统：初始化/控制台输出/文件输出/自动前缀/幂等性
│   ├── test_exceptions.py          # @safe_call装饰器：正常执行/异常默认值/重新抛出/指定捕获/日志级别
│   ├── test_event_bus.py           # 事件路由系统：信号连接/视图切换/文件保存/标签计数/文件移动
│   ├── test_timer_manager.py       # 定时器管理：创建定时器/更新间隔/停止/回调执行/异常处理
│   ├── test_game_engine.py         # 挂机收益引擎：在线奖励/铝材计数/倍率/离线奖励/时间上限
│   ├── test_menu_builder.py        # 菜单构建器：六菜单创建/文件菜单/编辑菜单/动作绑定
│   ├── test_editor_actions.py      # 编辑器行操作：删除行/复制行/上移行/下移行
│   ├── test_auto_pair_handler.py   # 括号自动配对：英文配对/中文配对/右括号跳过/设置开关
│   ├── test_config.py              # 配置管理：初始化/编辑器设置/游戏设置/资源CRUD/打字统计/离线时间
│   ├── test_savegame_manager.py    # 存档管理器：加载/保存/加密状态/SavegameSaveResult/解锁数据恢复/加密文件备份/每日签到
│   ├── test_first_run_dialog.py    # 首次运行对话框：初始化/默认路径/确认创建目录
│   ├── test_game_sidebar.py        # 游戏侧边栏：按钮初始化/当前视图/视图切换信号
│   ├── test_highlight_themes.py    # 代码高亮主题：主题列表/获取主题/格式构建/预览CSS/代码高亮HTML
│   ├── test_resource_bar.py        # 资源栏：资源项初始化/数值设置/颜色映射/刷新/打字统计
│   ├── test_secretary_widget.py    # 小秘书组件：台词气泡/消息显示/事件台词/立绘格式化
│   ├── test_status_bar.py          # 状态栏：默认标签/统计更新/编码设置/文件类型设置
│   ├── test_syntax_highlighter.py  # 语法高亮器：Markdown标题/代码块/行内代码/粗体/链接/列表/引用
│   ├── test_dpi_helper.py          # 高DPI缩放：缩放因子/整数缩放/尺寸缩放/字体缩放/样式表缩放/幂等性
│   ├── test_error_handler.py       # 错误提示系统：路径过滤/堆栈过滤/IP过滤/密码过滤/分类标签/建议映射
│   ├── test_shortcut_manager.py    # 快捷键管理：初始化/默认加载/注册/回调/冲突检测/持久化
│   ├── test_path_validator.py      # 路径安全验证：路径规范化/目录穿越检测/白名单/路径长度/控制字符
│   ├── test_file_guard.py          # 文件操作安全：初始化/安全读写/字节读写/大小限制/超时控制
│   ├── test_crypto_manager.py      # 存档加密系统：加密解密/错误密码/损坏数据/中文数据/文件操作/迁移
│   ├── test_input_validator.py     # 输入验证框架：文件名验证/保留名/搜索注入/设置值验证/清洗接口
│   ├── test_plugin_system.py       # 插件基础：状态枚举/权限枚举/元数据CRUD/基类生命周期/沙箱API/权限检查
│   ├── test_plugin_manager.py      # 插件管理器：扫描/加载/激活/停用/卸载/热加载/清单验证/版本兼容
│   ├── test_theme_system.py        # 主题系统：颜色方案/布局配置/主题定义/引擎加载/QSS生成/预览对话框
│   ├── test_feature_flags.py       # Feature Flag系统：已注册flag查询/未注册flag警告/默认值/配置持久化
│   └── test_virtual_scroll.py      # 虚拟滚动管理器：大文件加载/异常回退/边界情况/模式切换
│
├── src/                            # ══════ 源代码 ══════
│   ├── __init__.py                 # 版本号唯一真相源（__version__/get_version/get_version_tuple）
│   ├── main_window.py              # 主窗口（菜单栏 + 布局组装 + 事件路由 + 插件/主题集成 + v1.6.6拖放白名单/分屏语义优化/封装修复）
│   │
│   ├── core/                       # ── 核心模块 ──
│   │   ├── __init__.py
│   │   ├── config.py               # 配置管理中枢（settings/workspace 读写 + SavegameManager/SecurityManager 组合委托）
│   │   ├── savegame_manager.py     # 存档管理器（加载/保存/加密状态/SavegameSaveResult枚举/每日签到）
│   │   ├── security_manager.py     # 安全管理器（PathValidator/FileGuard/InputValidator集成管理）
│   │   ├── timer_manager.py        # 定时器管理中心（自动保存/统计/挂机奖励定时器）
│   │   ├── event_bus.py            # 事件路由系统（信号连接集中管理）
│   │   ├── menu_builder.py         # 菜单构建器（已接入ShortcutManager，v1.6.6分屏菜单语义更新）
│   │   └── shortcut_manager.py     # 快捷键管理器（注册/冲突检测/自定义/持久化）
│   │
│   ├── editor/                     # ── 编辑器模块 ──
│   │   ├── __init__.py
│   │   ├── editor.py               # 核心编辑器（行号、缩略图、语法高亮、自动缩进，v1.6.6新增_is_pasting粘贴检测/Backspace优化）
│   │   ├── editor_tabs.py          # 多标签管理（打开/保存/关闭/编码检测/拖拽/文件移动/安全读写，v1.6.6 IME粘贴误判修复/阈值提升/异步保存）
│   │   ├── editor_actions.py       # 行操作/大小写转换/JSON/XML格式化（Mixin）
│   │   ├── auto_pair_handler.py    # 括号/引号自动配对（Mixin，v1.6.6性能优化：frozenset快速过滤/_doc_char_at/_wrap_selection）
│   │   ├── save_task.py            # 后台文件保存任务（v1.6.6新增：SaveTask + SaveTaskSignals + QThreadPool异步写入）
│   │   ├── virtual_scroll.py       # 延迟高亮管理器（大文件延迟语法高亮）
│   │   ├── async_highlight.py      # 异步代码高亮渲染器（QThread + 任务队列）
│   │   ├── incremental_renderer.py # 渲染缓存（MD5哈希缓存，全文级）
│   │   ├── syntax_highlighter.py   # 语法高亮（Pygments适配器 + Markdown专用高亮器）
│   │   ├── highlight_themes.py     # 代码高亮主题（PyCharm Light 配色，编辑器 + 预览统一配色）
│   │   ├── markdown_preview.py     # Markdown分屏（PreviewBrowser + 代码块高亮 + 本地图片 + 异步渲染，v1.6.6 MarkdownIt实例复用/JS增量更新/主题修复）
│   │   ├── minimap.py              # 代码缩略图（v1.6.6 块级缓存增量失效：contentsChange + _block_dirty）
│   │   ├── find_replace.py         # 查找替换栏（正则/大小写/全词 + ExtraSelections高亮 + 搜索验证）
│   │   ├── editor_settings_dialog.py # 记事本设置对话框
│   │   ├── file_tree.py            # 文件树（QFileSystemModel + 拖拽移动文件 + 输入验证）
│   │   └── status_bar.py           # 状态栏（行列位置 + 字符数 + 编码 + 文件类型）
│   │
│   ├── game/                       # ── 游戏模块 ──
│   │   ├── __init__.py
│   │   ├── game_engine.py          # 挂机收益计算引擎（在线/离线奖励、资源上限检查）
│   │   ├── resource_bar.py         # 资源栏UI（四色图标 + 千分位数值 + 打字统计）
│   │   ├── game_sidebar.py         # 游戏侧边栏（返回/建造/车库/图鉴 四个占位按钮）
│   │   └── secretary_widget.py     # 小秘书（立绘 + 台词气泡 + 事件台词 + 角色切换 + 百分比尺寸）
│   │
│   ├── ui/                         # ── UI组件 ──
│   │   ├── __init__.py
│   │   ├── first_run_dialog.py     # 首次运行（选择数据存储路径 + 创建目录结构）
│   │   └── shortcut_panel.py       # 快捷键提示面板（搜索/分类展示/双击编辑）
│   │
│   ├── security/                   # ── 安全模块 ──
│   │   ├── __init__.py             # 安全模块导出与异常定义
│   │   ├── path_validator.py       # 路径安全验证（规范化/白名单/穿越防护/Windows特性）
│   │   ├── file_guard.py           # 文件操作安全控制（大小限制/超时控制/符号链接处理）
│   │   ├── crypto_manager.py       # 存档加密系统（PBKDF2+AES-GCM/迁移/备份/密码验证）
│   │   ├── input_validator.py      # 输入验证框架（文件名/搜索/设置值/清洗接口）
│   │
│   ├── plugins/                    # ── 插件系统 ──
│   │   ├── __init__.py             # 插件系统导出
│   │   ├── plugin_base.py          # 插件基类与元数据定义（生命周期/权限枚举/PluginMeta，min_app_version引用集中版本）
│   │   ├── plugin_sandbox.py       # 插件包装器（线程隔离/超时/权限控制/PluginAPI，v1.6.6安全加固：_mvp_read_only实例属性/setdefault原子注册）
│   │   ├── plugin_manager.py       # 插件管理器（扫描/加载/激活/热加载/生命周期管理）
│   │   └── plugin_manager_dialog.py # 插件管理对话框
│   ├── themes/                     # ── 主题系统 ──
│   │   ├── __init__.py             # 主题系统导出
│   │   ├── theme_engine.py         # 主题引擎（JSON/YAML加载/颜色解析/QSS生成/主题切换）
│   │   ├── theme_aware_mixin.py    # 主题感知混入（组件订阅theme_changed信号，自动更新样式）
│   │   └── theme_preview.py        # 主题预览对话框（实时预览/主题选择）
│   │
│   └── utils/                      # ── 工具模块 ──
│       ├── __init__.py             # 工具模块导出
│       ├── logger.py               # 结构化日志系统（控制台 + 滚动文件）
│       ├── exceptions.py           # 统一异常处理 @safe_call 装饰器
│       ├── error_handler.py        # 统一错误提示系统（8类分类/敏感信息过滤/自定义处理器）
│       ├── dpi_helper.py           # 高DPI缩放适配（AA_EnableHighDpiScaling时为no-op，否则手动缩放）
│       ├── feature_flags.py        # Feature Flag 系统（5个性能优化开关，未注册flag警告日志）
│       └── lazy_loader.py          # 启动性能分析（StartupProfiler阶段计时，LazyLoader已移除）
│
├── scripts/                        # ══════ 工具脚本 ══════
│   └── verify_version.py           # 版本一致性验证工具（5项检查/自动修复文档版本号）
│
├── data/                           # ══════ 数据 & 资源（程序目录） ══════
│   ├── assets/
│   │   ├── icons/
│   │   │   └── app_icon.png        # 应用图标
│   │   └── portraits/              # 角色立绘（见下方架构说明）
│   │       ├── secretary.png       # 默认小秘书立绘
│   │       ├── 原始/正常/          # 规划中：{编号} {角色名}-正常.png
│   │       ├── 原始/大破/          # 规划中：{编号} {角色名}-大破.png
│   │       ├── 皮肤/正常/          # 规划中：{编号} {角色名} {皮肤名}-正常.png
│   │       └── 皮肤/大破/          # 规划中：{编号} {角色名} {皮肤名}-大破.png
│   └── gamedata/
│       ├── characters.json         # 角色数据库（10个示例角色，含阈值/建造时间/稀有度）
│       └── secretary_lines.json    # 小秘书台词配置
│
├── plugins/                        # ══════ 插件目录 ══════
│   ├── hello_panzer/               # 基础功能示例插件
│   │   ├── plugin.json             # 插件清单（名称/版本/权限/入口）
│   │   └── main.py                 # 插件入口（PluginBase子类）
│   ├── word_counter/               # UI扩展示例插件
│   │   ├── plugin.json             # 插件清单
│   │   └── main.py                 # 插件入口
│   └── plugin_api.md               # 插件开发技术文档
│
├── notebooks/                      # 用户笔记库（用户数据路径下，首次运行创建）
│
│   ── 以下目录位于用户数据路径（非项目源码目录），由 ensure_directories() 创建 ──
│
│   {用户数据路径}/
│   ├── notebooks/                  # 用户笔记库（工作/回忆/日记）
│   ├── data/config/
│   │   ├── settings.json           # 编辑器/游戏/小秘书/视图/窗口设置
│   │   └── workspace.json          # 会话状态（打开的文件列表、最近文件、外部文件）
│   ├── data/gamedata/
│   │   └── savegame.json           # 游戏存档（资源、核心、打字统计、建造队列、已拥有角色）
│   └── temp/autosave/              # 自动保存暂存区（运行时创建，退出时清理）
```

---

## 4. 核心模块详解

### 4.1 配置管理 (`core/config.py`)

Config 类是整个应用的配置中枢，通过组合方式委托 `SavegameManager` 和 `SecurityManager` 管理存档和安全功能，自身负责 settings/workspace 两个 JSON 文件：

| 文件             | 位置                         | 职责                                                           | 管理者            |
| ---------------- | ---------------------------- | -------------------------------------------------------------- | ----------------- |
| `settings.json`  | `{base_path}/data/config/`   | 所有用户偏好设置（编辑器、游戏、小秘书、视图、窗口）           | `Config`          |
| `workspace.json` | `{base_path}/data/config/`   | 会话状态（打开的文件列表、最近文件、外部文件、文件树展开状态） | `Config`          |
| `savegame.json`  | `{base_path}/data/gamedata/` | 游戏存档（四资源、核心数、打字统计、建造队列、拥有角色、成就） | `SavegameManager` |

**路径机制（方案A）**：

- 程序目录下保存 `user_data_path.txt`，记住用户选择的数据存储路径
- 首次运行由 `FirstRunDialog` 引导选择
- 资源文件（立绘、图标等）始终从**程序目录** `_app_dir` 读取
- 用户数据（笔记、配置、存档）从 `_base_path` 读取

**关键方法**：

- `_merge_dict(default, current)` — 递归合并，确保新增配置项向下兼容
- `get_resources()` / `add_resource()` — 资源 CRUD（委托 SavegameManager）
- `get_today_chars_typed()` — 自动按日期重置计数器（委托 SavegameManager）
- `update_last_login()` — 离线收益的时间基准（委托 SavegameManager）

### 4.2 主窗口 (`main_window.py`)

**布局结构**（从上到下、从左到右）：

```
┌──────────────────────────────────────────────────────┐
│  ResourceBar（资源栏：燃料/弹药/钢材/铝材 + 统计）    │
├──────────────────────────────────────────────────────┤
│  GameSidebar │ QSplitter                             │
│  (50px固定)  │ ┌─────────┬─────────────────────────┐ │
│  ← 返回      │ │FileTree │ FindReplaceBar (隐藏)   │ │
│  建 建造     │ │(可折叠)  │ EditorTabWidget          │ │
│  库 车库     │ │          │  ┌───┬───┬───┐           │ │
│  鉴 图鉴     │ │          │  │Tab│Tab│Tab│           │ │
│              │ │          │  ├───┴───┴───┤           │ │
│              │ │          │  │ Editor /   │           │ │
│              │ │          │  │ MD Preview │           │ │
│              │ │          │  │        ┌──┐│           │ │
│              │ │          │  │   小秘书│  ││           │ │
│              │ └─────────┴──┴────────┴──┘│           │ │
├──────────────┴──────────────────────────────────────┤
│  StatusBar（行/列 | 字符数 | 编码 | 文件类型）        │
└──────────────────────────────────────────────────────┘
```

**拆分后的模块职责**：

| 模块                    | 职责                                                                                    |
| ----------------------- | --------------------------------------------------------------------------------------- |
| `main_window.py`        | 布局组装、菜单动作代理、状态管理、插件/主题集成、v1.6.6拖放白名单/分屏语义优化/封装修复 |
| `core/timer_manager.py` | 定时器生命周期管理（自动保存/统计/挂机奖励）                                            |
| `core/event_bus.py`     | 信号连接集中管理，解耦模块间通信                                                        |
| `core/menu_builder.py`  | 菜单栏构建逻辑，已接入ShortcutManager，支持快捷键可定制                                 |
| `game/game_engine.py`   | 挂机收益计算（在线/离线奖励、打字奖励、资源上限检查）                                   |

**定时器**（由 `TimerManager` 管理）：

- `auto_save_timer`（默认30秒）→ 调用 `save_all_to_temp()`
- `stats_timer`（500ms）→ 更新状态栏和资源栏统计
- `idle_reward_timer`（60秒）→ 在线挂机资源发放

**挂机机制**（由 `GameEngine` 计算）：

- **在线**：每分钟 fuel/ammo/steel +5，bauxite 每3分钟+5（用 `bauxite_counter` 计数）
- **离线**：启动时 `GameEngine.calculate_offline_rewards()` 根据 `last_login` 计算时间差，收益 = 在线的 1/3（向大取整），上限24h，最少5分钟

### 4.3 编辑器 (`editor/editor.py`)

基于 `QPlainTextEdit`，使用 Mixin 模式组合功能。类继承：`Editor(AutoPairHandlerMixin, EditorActionsMixin, QPlainTextEdit)`

**拆分后的模块职责**：

| 模块                          | 职责                                                                                 |
| ----------------------------- | ------------------------------------------------------------------------------------ |
| `editor/editor.py`            | 核心编辑器（行号、缩略图、语法高亮、自动缩进、虚拟滚动、v1.6.6粘贴检测）             |
| `editor/editor_actions.py`    | 行操作、大小写转换、JSON/XML格式化（Mixin，v1.6.6 \_pick_single_cjk_quote优化）      |
| `editor/auto_pair_handler.py` | 括号/引号自动配对（Mixin，v1.6.6性能优化：frozenset/\_doc_char_at/\_wrap_selection） |
| `editor/save_task.py`         | 后台文件保存任务（v1.6.6新增：SaveTask + QThreadPool异步写入）                       |
| `editor/virtual_scroll.py`    | 延迟高亮管理器（大文件延迟语法高亮）                                                 |

主要功能块：

| 功能区       | 说明                                                                                                                                                                                                                                   |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **行号**     | `LineNumberArea` 子控件，重写 `paintEvent` 绘制行号，动态计算宽度                                                                                                                                                                      |
| **缩略图**   | 内嵌 `MinimapWidget`，通过 `_update_child_geometries()` 管理几何位置                                                                                                                                                                   |
| **语法高亮** | 调用 `get_highlighter_for_file()` 工厂函数，支持 Pygments 和内置 Markdown                                                                                                                                                              |
| **自动缩进** | `_handle_enter()` 保持缩进 + 检测 `:`/`{` 等触发额外缩进                                                                                                                                                                               |
| **虚拟滚动** | `VirtualScrollManager` 管理大文件延迟语法高亮，大文件打开时延迟 rehighlight                                                                                                                                                            |
| **行操作**   | `EditorActionsMixin`：删除行 / 复制行 / 上下移动行，通过 `beginEditBlock/endEditBlock` 保证 undo 原子性                                                                                                                                |
| **大小写**   | `EditorActionsMixin`：`toggle_case` / `to_uppercase` / `to_lowercase` / `to_titlecase`                                                                                                                                                 |
| **格式化**   | `EditorActionsMixin`：JSON (`json.dumps`) 和 XML (`minidom.toprettyxml`) 格式化                                                                                                                                                        |
| **括号配对** | `AutoPairHandlerMixin`：`keyPressEvent` + `inputMethodEvent` 双路径处理英文和中文 IME 输入。v1.6.6 性能优化：frozenset O(1)快速过滤非配对字符；`_doc_char_at()` 单字符访问替代全文复制；`_wrap_selection()` cursor操作替代大字符串复制 |
| **右键菜单** | 全中文化菜单，包含大小写子菜单和格式化选项                                                                                                                                                                                             |

**括号配对的复杂性**：

- 支持英文 `() [] {} "" ''` 和中文 `（）【】「」『』《》〈〉""''`
- `keyPressEvent` 处理英文键盘直接输入
- `inputMethodEvent` 处理中文 IME 提交的字符（中文标点走这个路径）
- 智能判断：左右均有非空白字符且不是已有配对 → 不配对
- 嵌套输入：已有配对符号之间输入新配对符号 → 正常补全
- 引号智能：通过 `_pick_single_cjk_quote()` 用未闭合引号栈判断应插入左/右引号（v1.6.6：改用 QTextCursor 读取前缀，替代 toPlainText() + 切片）
- Backspace 成对删除（v1.6.6：改用 `_doc_char_at()` 单字符访问，替代全文复制）
- 选中文本时自动包裹（v1.6.6：改用 `_wrap_selection()` cursor 操作，替代 selectedText() 大字符串复制）

**v1.6.6 性能优化详解**：

| 优化项     | 旧实现                                | 新实现                                      | 性能影响                |
| ---------- | ------------------------------------- | ------------------------------------------- | ----------------------- |
| 入口过滤   | 每次按键都获取光标、读取字符          | `frozenset` O(1) 判断字符是否属于配对字符集 | 非配对字符零开销        |
| 字符访问   | `toPlainText()` 全文复制 → 索引取字符 | `_doc_char_at(pos)` 直接访问文档单字符      | O(n) → O(1)，大文件显著 |
| 选区包裹   | `selectedText()` 复制选区文本         | `_wrap_selection()` cursor 操作             | 避免大字符串复制        |
| 引号栈扫描 | `toPlainText()` + 切片                | `QTextCursor` 选择范围 + `selectedText()`   | 仅扫描前缀，不复制全文  |
| 行内计数   | 全文切片 + `str.count()`              | `block.text()` + `str.count(start, end)`    | 仅读取当前行            |

### 4.4 标签页管理 (`editor/editor_tabs.py`)

| 组件              | 说明                                                                                  |
| ----------------- | ------------------------------------------------------------------------------------- |
| `DraggableTabBar` | 继承 `QTabBar`，标签内拖拽 = 重排序，拖出标签栏 = 发起 `QDrag`（携带文件路径 MIME）   |
| `EditorTabWidget` | 继承 `QTabWidget`，管理 `_tab_info` 字典（filepath/modified/encoding/is_markdown 等） |
| `SaveAsDialog`    | 自定义另存为对话框，支持编码选择（UTF-8/GBK/UTF-16）                                  |

**核心逻辑**：

- `open_file()` — 编码级联检测（UTF-8 → GBK → UTF-16 → 容错UTF-8），Markdown 文件自动使用 `MarkdownPreviewWidget`
- `_on_text_changed()` — 比较当前内容与 `last_saved_content`，决定是否标记为已修改（标签名加 ` *`）。v1.6.6：新增 `is_pasting` 检查，粘贴操作不计入打字奖励；粘贴检测阈值 `_PASTE_THRESHOLD` 从 15 提升至 50
- `move_file_to_folder()` — 先保存最新内容 → `shutil.move` → 更新 `_tab_info` 中的 filepath
- 所有编辑操作（undo/redo/cut/copy/paste/行操作/大小写/格式化）通过代理方法转发给当前编辑器

### 4.5 Markdown 预览 (`editor/markdown_preview.py`)

**渲染管线**：

```
编辑器文本 → markdown 库渲染 HTML → _process_code_blocks()（Pygments内联样式高亮 + 浅蓝容器 + Unicode标记）
           → _resolve_local_images()（相对路径 → file:// 绝对路径）
           → PREVIEW_HTML_TEMPLATE 包裹 → QTextBrowser/QWebEngineView 显示
```

**v1.6.2 新增异步渲染管线**（Feature Flag `async_highlight` 控制）：

```
编辑器文本 → markdown 库渲染 HTML → _process_code_blocks_async()（代码块先用纯文本占位）
           → AsyncHighlightRenderer 后台线程渲染代码高亮
           → _on_async_highlight_done() 信号回调替换占位符为高亮结果
```

**增量渲染**（Feature Flag `markdown_incremental` 控制）：

- `IncrementalRenderer` 基于文本 MD5 哈希缓存渲染结果，相同文本直接返回缓存（全文级缓存，非行级增量）

**浮动复制按钮** (`PreviewBrowser`)：

- 在每个代码块 HTML 首尾嵌入不可见 Unicode 标记（⌜N⌝ / ⌞N⌟）
- `setHtml` 后用 `QTextDocument.find()` 缓存标记的 QTextCursor
- `mouseMoveEvent` 判断鼠标是否在某代码块垂直范围内，是则显示浮动复制按钮
- 防抖 30ms + 按钮 enter/leave 处理防止闪烁

**v1.6.6 主题修复**：

- `_apply_theme_colors(colors)` 增加 `isinstance(self.preview, PreviewBrowser)` 类型检查
- 修复主题初始化顺序导致的 `AttributeError`：当 `preview` 尚未创建为 `PreviewBrowser` 实例时（如 `QTextBrowser` 退化模式），不再尝试访问 `_copy_btn` 属性

**v1.6.6 MarkdownIt 实例复用**（PERF-002）：

- 新增 `_create_md_parser()` 方法，在 `__init__` 中创建一次 MarkdownIt 解析器实例并缓存为 `_md_parser`
- `_render_markdown()` 直接调用缓存的 `_md_parser.render()`，消除每次按键重复实例化 + 插件注册的开销
- deflist 插件 ImportError 增加 debug 日志，不再静默忽略

**v1.6.6 预览增量更新**（PERF-007）：

- `PREVIEW_HTML_TEMPLATE` 新增 `<div id="content">` 包裹内容区
- 新增 `_html_template_loaded` 标志：首次渲染走 `setHtml` 加载完整模板，`loadFinished` 信号触发后标记为 True
- 后续渲染通过 `QWebEngineView.page().runJavaScript()` 仅更新 `document.getElementById('content').innerHTML`
- 切换文档（`base_path` 变化）时自动重置 `_html_template_loaded = False`，强制下次全量加载
- 非 WebEngine 模式（QTextBrowser）仍走全量 `setHtml` 路径

### 4.6 代码高亮主题 (`editor/highlight_themes.py`)

当前仅实现 `pycharm_light` 主题（与 theme_engine 的浅色/深色主题不联动），但架构支持扩展：

- `THEMES` 字典：每个主题 = `{Token: {color, bold, italic, underline, background}}`
- 编辑器端：`get_editor_formats()` → `{Token: QTextCharFormat}`
- 预览端：`highlight_code_html()` → Pygments HtmlFormatter + 内联 style
- 新增主题只需在 `THEMES` 中增加条目 + `settings.json` 中 `editor.code_highlight_theme` 切换

### 4.7 小秘书 (`game/secretary_widget.py`)

- 固定在 `editor_container` 右下角（通过 `eventFilter` 监听父容器 `Resize`/`Move` 事件）
- 防抖机制：位置更新请求通过 `_position_timer`（≤50ms 间隔）合并，避免频繁重绘
- 动态位置计算：`_calculate_target_position()` 基于 margin 值，确保立绘右下对齐且不越界
- 百分比尺寸控制：`_apply_size()` 根据父容器面积和 `size_percent`（默认 7%，范围 3%~20%）动态计算宽高，保持 210:380 宽高比
- `set_size_percent(percent)` / `get_size_percent()` — 运行时调节尺寸，自动持久化到 `settings.json`
- 窗口 resize 时自动重新计算尺寸和位置
- 台词系统：`secretary_lines.json` 定义多种事件（启动/保存/建造/闲置/点击等），支持 `{nickname}` 和 `{self}` 占位符
- 立绘加载：按 `character_id/character_name/skin_name/state` 构建路径，fallback 到 `secretary.png`

### 4.8 高 DPI 缩放 (`utils/dpi_helper.py`)

- `init_dpi(app)` — 检测 `Qt.AA_EnableHighDpiScaling` 是否已启用：
  - 若已启用：`scale_factor = 1.0`（Qt 自动处理 DPI 缩放，dpi_helper 所有 scale 函数为 no-op）
  - 若未启用：基于 `logicalDotsPerInch` / `devicePixelRatio` 计算缩放因子
- **注意**：当前 `main.py` 已启用 `AA_EnableHighDpiScaling`，因此 dpi_helper 在生产环境中实际为 no-op
- `scale(value)` — 将基准像素值乘以缩放因子，最小返回 1
- `scale_size(w, h)` — 缩放 QSize
- `scale_font(pt)` — 缩放字体大小，最小 8pt
- `scale_stylesheet(css)` — 正则替换样式表中所有 `Npx` 值
- `dp(value)` — dp 单位转换（1dp = 缩放因子像素）

### 4.9 快捷键管理 (`core/shortcut_manager.py`)

- `ShortcutManager(config)` — 管理所有快捷键的注册、冲突检测、自定义和持久化
- `register(action_id, name, default_shortcut, callback, category)` — 注册快捷键，返回 QAction
- `check_conflicts(key_sequence, exclude)` — 检测系统级（Ctrl+C/V 等）和应用内部冲突
- `set_shortcut(action_id, key_sequence)` — 修改快捷键并持久化到 `settings.json`
- `reset_shortcut(action_id)` / `reset_all()` — 恢复默认快捷键
- `get_categories()` / `get_all_shortcuts()` — 获取分类和完整列表
- `_normalize_key(key)` — 统一快捷键格式（小写 + 修饰键排序），用于冲突比较
- `_NORMALIZED_SYSTEM_SHORTCUTS` — 预构建的规范化系统快捷键字典，确保与用户输入格式一致
- **注意**：ShortcutManager 已接入 MenuBuilder，所有菜单项通过 `manager.register()` 注册，自定义快捷键功能已生效

### 4.10 统一错误提示 (`utils/error_handler.py`)

- `ErrorCategory` 枚举：FILE / NETWORK / CONFIG / GAME / EDITOR / PERMISSION / MEMORY / GENERAL
- `ErrorHandler.show_error(category, title, message, suggestion, detail)` — 显示用户友好错误提示
- `ErrorHandler.show_from_exception(exception, category, title)` — 从异常对象生成提示
- **注意**：当前仅 `config.py` 中 8 处使用 `@safe_call`，其余模块仍用裸 try/except
- `ErrorHandler.sanitize(text)` — 公共接口，过滤敏感信息
- `_sanitize_message(text)` — 内部过滤函数，使用正则匹配：Windows/Unix/Mac 路径、Traceback、IP 地址、密码/Token/Key
- `_ErrorDialog` — 默认错误对话框，使用 QMessageBox，样式与应用统一
- 自定义处理器：`register_handler(category, handler)` / `unregister_handler(category)`，支持回退

### 4.11 安全模块 (`security/`)

阶段四新增的安全防护体系，包含五个核心模块，集成到 `config.py`、`editor_tabs.py`、`file_tree.py`、`find_replace.py`、`main_window.py` 中。

#### 4.11.1 路径安全验证 (`security/path_validator.py`)

- `PathValidator` — 路径安全验证器，提供白名单机制和目录穿越防护
- `normalize(path)` — 使用 `os.path.realpath` 规范化路径，处理 Windows `\\?\` 长路径前缀
- `add_allowed_root(root_path)` — 添加安全路径白名单（大小写不敏感）
- `is_path_safe(path)` — 综合安全检查：空值/类型/长度/穿越/白名单
- `validate_path(path)` — 验证并返回规范化路径，失败抛出 `PathSecurityError`/`PathTraversalError`/`PathNotInWhitelistError`
- `is_path_traversal(path)` — 检测 `../`/`..\\` 等穿越模式，包含控制字符检测
- `is_path_in_whitelist(path)` — 白名单校验，Windows 大小写不敏感比较
- `_case_normalize(path)` — Windows 下转小写，其他平台保持原样
- `MAX_PATH_LENGTH = 260` — 路径长度上限

**安全异常层次**：

```
PathSecurityError (基类)
├── PathTraversalError    — 目录穿越攻击
└── PathNotInWhitelistError — 路径不在白名单
```

#### 4.11.2 文件操作安全控制 (`security/file_guard.py`)

- `FileGuard` — 文件操作安全守卫，集成路径验证、大小限制和超时控制
- `safe_read(filepath, encoding, validate_path)` — 安全读取文件，支持路径验证、大小检查、超时中断
- `safe_read_bytes(filepath, validate_path)` — 安全读取二进制文件
- `safe_write(filepath, content, encoding, validate_path)` — 安全写入文件，自动创建目录
- `check_file_size(filepath)` — 检查文件大小是否超限
- `get_real_file_size(filepath)` — 获取真实文件大小（处理符号链接和稀疏文件）
- `_get_disk_size(filepath)` — 使用 `os.stat` 获取实际磁盘占用（非表观大小）
- `DEFAULT_MAX_FILE_SIZE = 50MB` — 默认文件大小上限
- `DEFAULT_TIMEOUT = 30s` — 默认操作超时阈值
- 超时机制：使用 `threading.Thread(daemon=True)` 执行文件操作，`join(timeout)` 控制超时

**安全异常层次**：

```
FileSecurityError (基类)
├── FileSizeExceededError      — 文件大小超限
└── FileOperationTimeoutError  — 文件操作超时
```

#### 4.11.3 存档加密系统 (`security/crypto_manager.py`)

- `CryptoManager` — 存档数据加密管理器，基于 PBKDF2 + AES-GCM
- `encrypt_data(password, data)` — 加密数据字典，返回加密字节串
- `decrypt_data(password, encrypted)` — 解密数据，返回原始字典
- `encrypt_savegame(password, data)` — 加密并保存到 `.encrypted` 文件
- `decrypt_savegame(password)` — 从加密文件读取并解密
- `is_encrypted()` — 检测存档是否已加密
- `verify_password(password)` — 验证密码正确性
- `migrate_to_encrypted(password)` — 未加密 → 加密格式迁移（自动备份 + 数据验证）
- `migrate_to_plaintext(password)` — 加密 → 未加密格式迁移
- `_derive_key(password, salt)` — PBKDF2-HMAC-SHA256 密钥派生（600,000 次迭代，32 字节密钥）
- `_save_salt(salt)` / `_load_salt()` — 盐值持久化到 `savegame.salt`
- `_backup_if_exists(filepath)` — 加密/迁移前自动备份（`.bak` 后缀）

**加密信封格式**：

```json
{
  "version": 1,
  "salt": "<base64>",
  "nonce": "<base64>",
  "data": "<base64 AES-GCM ciphertext>"
}
```

**安全异常层次**：

```
CryptoError (基类)
├── DecryptionError   — 解密失败
└── MigrationError    — 迁移失败
```

#### 4.11.4 输入验证框架 (`security/input_validator.py`)

- `InputValidator` — 统一输入验证器，覆盖文件名、搜索内容、设置值
- `validate_filename(filename)` — 宽松文件名验证（允许空扩展名）
- `validate_filename_strict(filename)` — 严格文件名验证（不允许点号开头/结尾）
- `sanitize_filename(filename)` — 清洗文件名（替换非法字符为下划线，截断长度）
- `validate_search(query)` — 搜索内容验证（XSS/注入模式检测，长度限制 10,000）
- `validate_setting(key, value, expected_type, min_val, max_val, allowed_values, max_length)` — 设置值验证
- `MAX_FILENAME_LENGTH = 255` / `MAX_SEARCH_LENGTH = 10000` / `MAX_SETTING_STRING_LENGTH = 1000`

**文件名验证规则**：

- 非法字符正则：`[<>:"|?*\\/\x00-\x1f]`
- Windows 保留名称：`CON`/`PRN`/`AUX`/`NUL`/`COM1-9`/`LPT1-9`
- 路径注入模式：`../`/`..\\`/`\\`/`/`
- 严格模式额外限制：不允许以点号开头或结尾

**搜索内容危险模式**：

- `<script>` / `javascript:` / `vbscript:` — 脚本注入
- `on\w+=` — 事件处理器注入
- `data:text/html` — 数据 URI 注入

**安全异常层次**：

```
ValidationError (基类)
├── FilenameValidationError  — 文件名验证失败
├── SearchValidationError    — 搜索内容验证失败
└── SettingValidationError   — 设置值验证失败
```

#### 4.11.5 安全管理器 (`core/security_manager.py`)

从 `Config` 类中拆分出的安全功能集成管理器，将 `PathValidator`、`FileGuard`、`InputValidator` 统一封装。

- `SecurityManager(path_validator, file_guard, input_validator)` — 安全管理器
- `path_validator` / `file_guard` / `input_validator` — 三个安全组件的只读属性
- `add_allowed_root(root_path)` — 添加路径白名单根目录
- `validate_setting_value(key, value, expected_type, ...)` — 设置值验证（委托 InputValidator）

#### 4.11.6 安全模块集成点

| 集成文件                 | 集成方式                                                                                                                                                                                                     |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `core/config.py`         | 初始化 `SecurityManager`（内含 PathValidator/FileGuard/InputValidator）和 `CryptoManager`；所有 JSON 读写通过 `FileGuard`；存档支持加密/解密；设置值通过 `InputValidator` 验证                               |
| `editor/editor_tabs.py`  | 文件打开通过 `FileGuard.safe_read()` 读取；处理 `FileSizeExceededError`/`FileOperationTimeoutError`                                                                                                          |
| `editor/file_tree.py`    | 新建文件/文件夹通过 `InputValidator.validate_filename_strict()` 验证；文件创建通过 `FileGuard.safe_write()`                                                                                                  |
| `editor/find_replace.py` | 搜索内容通过 `InputValidator.validate_search()` 验证，拒绝注入模式                                                                                                                                           |
| `main_window.py`         | v1.6.6 新增：拖放文件通过 `_SUPPORTED_DROP_EXTS` 白名单校验，非文本文件弹出警告；PDF/HTML导出均显式禁用 raw HTML（`MarkdownIt("commonmark", {"html": False})`），python-markdown fallback 仅启用 tables 扩展 |

#### 4.11.7 拖放文件类型白名单（v1.6.6 新增）

`MainWindow` 类新增 `_SUPPORTED_DROP_EXTS` 类属性，在 `dropEvent` 中对拖入的文件进行扩展名白名单校验。

- `_SUPPORTED_DROP_EXTS` — `frozenset`，包含允许拖入打开的文件扩展名：`.txt`、`.md`、`.py`、`.c`、`.cpp`、`.h`、`.java`、`.js`、`.json`、`.html`、`.css`、`.xml`、`.yaml`、`.yml`、`.toml`、`.ini`、`.log`、`.sql`、`.sh`、`.go`、`.rs`、`''`（无扩展名）
- `dropEvent()` — 遍历拖入的 URL，对每个本地文件：检查是否为文件（`os.path.isfile`）、提取扩展名（`os.path.splitext`）、白名单校验。非白名单扩展名弹出 `QMessageBox.warning` 提示"不支持的文件类型"
- `dragEnterEvent()` — 仅当拖入内容包含本地文件 URL 时接受拖拽（保持原有逻辑）

**安全意义**：防止用户意外将二进制文件（如 `.exe`、`.dll`、`.zip`、`.png` 等）拖入编辑器导致程序异常或显示乱码。

**扩展方式**：修改 `MainWindow._SUPPORTED_DROP_EXTS` 类属性即可添加新的文件类型支持，无需修改其他代码。

### 4.12 插件系统 (`plugins/`)

插件系统提供标准化的扩展机制，允许第三方开发者为应用添加功能。插件运行在独立线程中（非进程隔离沙箱），通过受限 API 与主程序交互。

#### 4.12.1 插件基类 (`plugins/plugin_base.py`)

- `PluginState` — 插件状态枚举（UNLOADED/LOADED/ACTIVATED/DEACTIVATED/ERROR）
- `PluginPermission` — 权限枚举（12种：READ_SETTINGS/READ_SAVEGAME/READ_WORKSPACE/READ_FILE_TREE/ACCESS_EDITOR/ACCESS_UI/ACCESS_NETWORK/ACCESS_FILESYSTEM/OPEN_FILE/SHOW_MESSAGE/REGISTER_COMMAND/GET_CONFIG）
- `PluginMeta` — 插件元数据（name/version/description/author/min_app_version/permissions/tags），`min_app_version` 默认值引用 `src.__version__`
- `PluginBase` — 插件基类，定义四个生命周期方法：`on_load(api)`/`on_activate()`/`on_deactivate()`/`on_unload()`

**状态转换规则**：

```
UNLOADED → on_load() → LOADED → on_activate() → ACTIVATED
ACTIVATED → on_deactivate() → DEACTIVATED → on_activate() → ACTIVATED
ACTIVATED → on_deactivate() → DEACTIVATED → on_unload() → UNLOADED
LOADED → on_unload() → UNLOADED
任意状态 → 异常 → ERROR
```

#### 4.12.2 插件包装器 (`plugins/plugin_sandbox.py`)

- `PluginAPI` — 受限 API 对象，每次调用检查权限声明
- `PluginSandbox` — 插件包装器，独立线程运行，最大超时30秒（非进程隔离沙箱）
- `SandboxViolationError` — 权限违规异常
- `SandboxTimeoutError` — 执行超时异常

**MVP 限制**：`ACCESS_NETWORK` 和 `ACCESS_FILESYSTEM` 权限被禁止，所有插件仅限只读访问。v1.6.6 安全加固：`MVP_READ_ONLY` 从类变量改为实例属性 `_mvp_read_only`，防止恶意插件通过类级别篡改；`register_command()` 使用 `dict.setdefault()` 原子操作，消除 TOCTOU 竞态条件。

**PluginAPI 接口**：

| 方法                                  | 所需权限           | 说明               |
| ------------------------------------- | ------------------ | ------------------ |
| `get_setting(key, default)`           | `READ_SETTINGS`    | 读取设置           |
| `get_editor_setting(key, default)`    | `READ_SETTINGS`    | 读取编辑器设置     |
| `get_game_setting(key, default)`      | `READ_SETTINGS`    | 读取游戏设置       |
| `get_secretary_setting(key, default)` | `READ_SETTINGS`    | 读取小秘书设置     |
| `get_resources()`                     | `READ_SAVEGAME`    | 读取游戏资源       |
| `get_savegame_field(key, default)`    | `READ_SAVEGAME`    | 读取存档字段       |
| `get_recent_files()`                  | `READ_WORKSPACE`   | 读取最近文件       |
| `get_notebooks_path()`                | `READ_FILE_TREE`   | 读取笔记库路径     |
| `get_app_version()`                   | 无                 | 获取应用版本       |
| `open_file(filepath)`                 | `OPEN_FILE`        | 打开文件到编辑器   |
| `show_message(message)`               | `SHOW_MESSAGE`     | 通过小秘书显示消息 |
| `register_command(name, callback)`    | `REGISTER_COMMAND` | 注册自定义命令     |
| `get_config(key, default)`            | `GET_CONFIG`       | 获取配置项         |

#### 4.12.3 插件管理器 (`plugins/plugin_manager.py`)

- `scan_plugins()` — 从 `plugins/` 目录递归扫描插件包
- `load_plugin(name)` — 加载插件（验证清单完整性、版本兼容性、创建沙箱API）
- `activate_plugin(name)` — 激活插件
- `deactivate_plugin(name)` — 停用插件
- `unload_plugin(name)` — 卸载插件
- `reload_plugin(name)` — 热加载插件（停用→卸载→清除缓存→重新加载→恢复状态）

**插件清单 (`plugin.json`) 必需字段**：`name`/`version`/`entry`

### 4.13 主题系统 (`themes/`)

主题系统支持外部主题文件加载，实现UI全局换肤。

#### 4.13.1 主题引擎 (`themes/theme_engine.py`)

- 支持 JSON/YAML 两种格式的外部主题文件
- 解析颜色方案（主色/背景/前景/强调色/边框等）
- 自动生成 QSS 样式表，覆盖所有 UI 元素
- 主题切换即时生效，无需重启

**主题文件格式示例**：

```json
{
  "name": "dark_theme",
  "display_name": "暗黑主题",
  "version": "1.0.0",
  "colors": {
    "background": "#1e1e2e",
    "foreground": "#cdd6f4",
    "accent": "#89b4fa",
    "border": "#45475a"
  }
}
```

#### 4.13.2 主题预览 (`themes/theme_preview.py`)

- `ThemePreviewDialog` — 主题预览对话框
- 实时预览主题效果
- 支持主题选择和切换

#### 4.13.3 主题感知混入 (`themes/theme_aware_mixin.py`)

- `ThemeAwareMixin` — 主题感知混入类，UI 组件继承后自动订阅 `theme_changed` 信号
- 组件需实现 `_apply_theme(theme)` 方法，在主题切换时更新自身样式
- 已集成组件（11个）：`Editor`、`MarkdownPreviewWidget`、`EditorTabWidget`、`MinimapWidget`、`FileTreeWidget`、`FindReplaceBar`、`StatusBarWidget`、`SecretaryWidget`、`ResourceBar`、`GameSidebar`、`ShortcutPanel`

### 4.14 数据存储抽象层（已删除）

原 `storage/` 目录已在架构整改中移除，原因：无业务调用方，IStorage 接口从未集成到实际数据流。相关测试文件 `test_storage.py` 已一并移除。如需存储抽象功能，应重新设计并与 `SavegameManager` 集成。

### 4.15 存档管理器 (`core/savegame_manager.py`)

从 `Config` 类中拆分出的独立存档管理模块，负责游戏存档的加载、保存和加密状态管理。

- `SavegameManager(gamedata_dir, crypto_manager, logger)` — 存档管理器
- `load()` — 加载存档（自动检测加密状态，加密存档需先 `set_encryption_password()` 解密）
- `save()` — 保存存档，返回 `SavegameSaveResult` 枚举值
- `set_encryption_password(password)` — 设置加密密码（用于解密和加密保存）
- `verify_encryption_password(password)` — 验证密码正确性
- `add_resource(key, amount)` / `get_resources()` — 资源 CRUD
- `is_encrypted_unread()` — 检测存档是否已加密但未解锁
- `_encrypted_unread` — 标记存档已加密但未解锁（未输入正确密码解密）
- `check_daily_checkin()` — 每日签到检查，首次启动自动发放奖励（燃料/弹药/钢材/铝材各+100）
- `_backup_encrypted_file()` — 加密存档安全备份（`.encrypted.bak`）

**SavegameSaveResult 枚举**：

| 值                         | 说明                                 |
| -------------------------- | ------------------------------------ |
| `SUCCESS`                  | 保存成功                             |
| `SKIPPED_ENCRYPTED_UNREAD` | 存档已加密但未解锁，跳过保存以防覆写 |
| `ENCRYPTION_FAILED`        | 加密保存失败，回退到明文保存         |

**加密存档保存流程**：

1. 检查 `_encrypted_unread` 标记，若为 True 则返回 `SKIPPED_ENCRYPTED_UNREAD`
2. 若有加密密码且存档已加密，尝试加密保存
3. 加密失败时回退到明文保存并返回 `ENCRYPTION_FAILED`
4. 非加密存档直接明文保存，返回 `SUCCESS`

**MainWindow 集成**：

- 关闭窗口时检查 `SavegameSaveResult`，若为 `SKIPPED_ENCRYPTED_UNREAD` 则弹出密码输入对话框
- 用户输入正确密码后解锁存档并重新保存
- 用户可选择放弃进度

### 4.16 集中式版本管理

版本号以 `src/__init__.py` 中的 `__version__` 为唯一真相源（Single Source of Truth），所有模块和配置文件通过引用获取版本号，而非硬编码。

**版本传播链路**：

```
src/__init__.py (__version__ = "1.6.6")
  ├─→ main.py                    (from src import __version__)
  ├─→ src/main_window.py         (from . import __version__)
  ├─→ src/plugins/plugin_base.py (from .. import __version__ as _app_version)
  │     └─→ PluginMeta.min_app_version 默认值
  ├─→ src/plugins/plugin_sandbox.py (from .. import __version__)
  ├─→ plugins/hello_panzer/main.py  (from src import __version__ as _app_version)
  ├─→ plugins/word_counter/main.py  (from src import __version__ as _app_version)
  └─→ pyproject.toml            (dynamic = ["version"] + attr = "src.__version__")
```

**工具函数**：

- `get_version()` — 返回版本号字符串
- `get_version_tuple()` — 返回版本号元组，如 `(1, 6, 6)`

**版本一致性验证** (`scripts/verify_version.py`)：

5 项检查：

1. `src/__init__.py` 版本格式（semver X.Y.Z）
2. `pyproject.toml` 动态版本配置（无静态 version 字段）
3. Python 文件中无硬编码应用版本号
4. 文档中非更新日志区域的版本号引用
5. 运行时版本一致性（`plugin_base._app_version` 与 `__version__` 匹配）

**启动时检查** (`main.py._verify_version_consistency()`)：

应用启动时自动验证 `plugin_base._app_version` 与 `src.__version__` 一致性，不一致时记录 WARNING 日志。

**版本更新流程**：

1. 仅修改 `src/__init__.py` 中的 `__version__`
2. 运行 `python scripts/verify_version.py` 验证一致性
3. 手动同步 `README.md` / `项目说明.md` / `plugins/plugin_api.md` 中的版本号

---

## 5. 数据文件详解

### 5.1 `characters.json` — 角色数据库

当前包含 10 个示例角色。每个角色结构：

```json
{
  "id": "char_001",
  "name": "虎王",
  "name_en": "Tiger II",
  "type": "heavy", // light/medium/heavy/super_heavy/td/spg
  "nation": "G", // G=德/S=苏/U=美/E=英/J=日/F=法/C=中/I=意
  "rarity": 5, // 1普通~6神话
  "build_time_minutes": 300,
  "threshold": {
    "total": 1800, // 四资源总投入最低门槛
    "fuel": 500, // 各项最低门槛（可选）
    "steel": 800,
    "bauxite": 200 // 某些特殊角色需要铝材门槛
  },
  "portrait": "...",
  "voice_intro": "..."
}
```

同时定义了 `types`（6种车型）、`nations`（8国）、`rarities`（6级稀有度 + 颜色）。

### 5.2 `savegame.json` — 游戏存档

```json
{
  "resources": { "fuel": 3000, "ammo": 3000, "steel": 3000, "bauxite": 1000 },
  "cores": 0,
  "last_login": "2025-01-01T12:00:00",
  "today_date": "2025-01-01",
  "today_chars_typed": 0,
  "total_chars_typed": 0,
  "total_documents": 0,
  "construction_queue": [], // 规划：建造队列
  "owned_characters": {}, // 规划：已拥有角色 {id: {count, first_obtained, ...}}
  "achievements": [] // 规划：成就系统
}
```

### 5.3 `settings.json` — 用户设置

分六个命名空间：`editor`（编辑器）、`game`（游戏）、`secretary`（小秘书）、`view`（视图）、`window`（窗口几何）、`shortcuts`（快捷键自定义）。所有设置都有 `DEFAULT_SETTINGS` 兜底默认值。

`shortcuts` 段示例：

```json
{
  "shortcuts": {
    "file.new": "Ctrl+N",
    "file.open": "Ctrl+O",
    "file.save": "Ctrl+S"
  }
}
```

仅保存用户自定义的快捷键覆盖，未修改的使用 `_DEFAULT_SHORTCUTS` 中的默认值。

---

## 6. 信号/事件流

### 6.1 文件打开流

```
用户双击文件树 / Ctrl+O
  → FileTreeWidget.file_open_requested(filepath)
  → MainWindow._open_file(filepath)
     → 判断是否笔记库外文件 → config.add_external_file()
     → EditorTabWidget.open_file(filepath)
        → 编码级联检测
        → 判断是否 Markdown → 创建 Editor 或 MarkdownPreviewWidget
        → 生成 tab_id，存入 _tab_info
        → set_file_type() → 绑定语法高亮 + auto_minimap
     → config.add_recent_file()
```

### 6.2 在线挂机资源发放

```
idle_reward_timer (每60秒，由 TimerManager 管理)
  → MainWindow._on_idle_reward()
     → GameEngine.calculate_idle_reward()
     → config.add_resource("fuel", 5)
     → config.add_resource("ammo", 5)
     → config.add_resource("steel", 5)
     → bauxite_counter++ → 满3时 add_resource("bauxite", 5)
     → resource_bar.refresh()
```

### 6.3 标签拖拽移动文件

```
DraggableTabBar.mouseMoveEvent (鼠标离开标签栏)
  → QDrag(MIME_TAB_FILEPATH = filepath UTF-8)
  → DroppableTreeView.dropEvent
     → 解析 MIME → 确定目标文件夹
     → FileTreeWidget.file_move_requested(src, dest)
     → MainWindow._on_file_move_from_tree(src, dest)
        → EditorTabWidget.move_file_to_folder(src, dest)
           → 保存最新内容 → shutil.move → 更新 _tab_info
        → secretary.show_message("已移动...")
```

---

## 7. 远期规划（未实现部分详述）

以下是开发者在项目开始前的原始设计意图，当前代码中已有部分数据结构预留但功能未实现。

### 7.1 打字奖励系统

**设计**：在编辑器的 `textChanged` 中接入有效击键统计，通过 `chars_typed` 信号传递字符增量到 `MainWindow._on_chars_typed()`，应用递减收益算法后调用 `GameEngine.add_typing_reward()` 转化为资源。

**规则**：

- 每日前 1000 字 100% 计入；1000–3000 字 40%；更高 10%（递减收益）
- 每日上限 10,000 字符
- 奖励按 1:1:1:0.2 比例分配到燃料/弹药/钢材/铝材

**代码现状**：已完成接入。`editor_tabs.py` 新增 `chars_typed` 信号，在 `_on_text_changed()` 中计算字符增量并发射；`MainWindow._on_chars_typed()` 实现递减收益算法；`GameEngine.add_typing_reward()` 将奖励转化为四项资源。v1.6.6 修复：`Editor` 新增 `_is_pasting` 标志（重写 `insertFromMimeData`），`_on_text_changed()` 增加 `is_pasting` 检查跳过粘贴操作；粘贴检测阈值从 15 提升至 50，避免 IME 整句输入误判。

### 7.2 建造系统

**设计**：

1. **输入**：用户输入四项资源配方 → 扣除资源 → 进入建造队列（默认2个槽位）
2. **等待时间**：与投入总量相关，可用加速券/核心跳过
3. **阈值过滤**：先做硬阈值过滤（总投入、单项最低要求），不够则该角色绝不出现
4. **概率抽取**：
   - 总投入越高 → 稀有池权重越高（有上限/平滑函数）
   - 配方比例影响倾向：Steel高→重型，Ammo高→火力型，Fuel高→机动型，Bauxite高→特殊型

**代码现状**：`characters.json` 已定义了10个角色（含 threshold 和 build_time_minutes），`savegame.json` 已有 `construction_queue` 和 `construction_slots` 字段，`game_sidebar.py` 已有"建造"按钮但视图切换后显示空白容器。

### 7.3 图鉴系统

**设计**：

- 未获得角色灰色/占位，获得后点亮
- 可查看立绘截图、播放语音（mp3）
- 重复获得转化为"核心/碎片"，可用于加速建造/扩容槽位/提高挂机效率

**代码现状**：`savegame.json` 有 `owned_characters` 和 `cores` 字段，`characters.json` 有完整的角色元数据和稀有度定义。

### 7.4 车库系统

**设计**：角色详情界面，查看已拥有角色的立绘、属性、获得记录。

**代码现状**：仅有侧边栏"库"按钮和空白 `game_view_container`。

---

## 8. 快捷键总览

| 分类   | 快捷键                                   | 功能                           |
| ------ | ---------------------------------------- | ------------------------------ |
| 文件   | `Ctrl+N/O/S/Shift+S/W`                   | 新建/打开/保存/另存为/关闭标签 |
| 编辑   | `Ctrl+Z/Y/X/C/V/A`                       | 撤销/重做/剪切/复制/粘贴/全选  |
| 查找   | `Ctrl+F/H/G`, `F3/Shift+F3`              | 查找/替换/转到行/下一个/上一个 |
| 行操作 | `Ctrl+Shift+K`, `Alt+↑↓`, `Ctrl+Shift+D` | 删除行/移动行/复制行           |
| 大小写 | `Ctrl+Shift+U`                           | 切换大小写                     |
| 视图   | `Ctrl+B/M/Shift+P`, `F11`, `Ctrl+±0`     | 文件树/缩略图/MD预览/全屏/缩放 |
| 导航   | `Ctrl+1/2/3/4`                           | 记事本/建造/车库/图鉴          |

---

## 9. 依赖与运行

### 运行依赖

```bash
pip install PyQt6>=6.8 PyQt6-WebEngine>=6.8 Pygments>=2.19 markdown>=3.8 Pillow>=12.2.0 cryptography>=48.0.0 send2trash>=2.1.0 markdown-it-py>=3.0.0
pip install PyQt6-WebEngine  # 可选，Markdown预览用QWebEngineView（否则退化为QTextBrowser）
python main.py
```

### 开发依赖

```bash
pip install pytest>=9.0 pytest-cov pytest-qt  # 单元测试
pip install mypy>=1.20                         # 类型检查
```

### 单元测试说明

项目共 29 个测试文件，覆盖核心模块、编辑器、游戏系统、安全模块及可扩展性架构。运行方式：`pytest tests/ -v`

| 测试文件                   | 测试目的                 | 测试范围                                                                 | 预期验证的功能点                                                                                                                        |
| -------------------------- | ------------------------ | ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| test_logger.py             | 验证结构化日志系统       | Logger 初始化、控制台/文件输出、幂等性                                   | 默认初始化创建 `src` 前缀 logger；文件日志写入 `panzernote.log`；重复调用 `setup_logging` 不重复添加 handler                            |
| test_exceptions.py         | 验证 `@safe_call` 装饰器 | 正常执行、异常默认值、重新抛出、指定捕获类型                             | 正常函数返回原值；异常时返回 `default`；`reraise=True` 重新抛出；`catch=ValueError` 仅捕获指定类型                                      |
| test_event_bus.py          | 验证事件路由系统         | 信号连接、视图切换、文件操作、标签管理                                   | `connect_signals` 连接所有 UI 信号；`handle_view_changed` 正确切换编辑器/建造视图；`handle_file_saved` 更新状态                         |
| test_timer_manager.py      | 验证定时器管理器         | 定时器创建、间隔更新、停止、回调执行                                     | `setup` 创建自动保存/统计/挂机三个定时器；`update_interval` 动态调整；`stop_all` 停止所有；回调异常不崩溃                               |
| test_game_engine.py        | 验证挂机收益引擎         | 在线奖励计算、铝材计数、倍率、离线奖励、时间上限                         | 基础奖励 fuel/ammo/steel 各 5；`bauxite_counter=0` 时铝材为 0；倍率乘算；离线 ≥5 分钟才计算；上限 24 小时                               |
| test_menu_builder.py       | 验证菜单构建器           | 六大菜单创建、菜单项内容、动作绑定                                       | 生成文件/编辑/游戏/视图/设置/帮助六个菜单；文件菜单含新建/打开/保存等；编辑菜单含行操作；动作触发对应方法                               |
| test_editor_actions.py     | 验证编辑器行操作         | 删除行、复制行、上移行、下移行                                           | 删除中间行正确移除；删除唯一行清空；复制行在下方插入副本；首行不可上移；末行不可下移                                                    |
| test_auto_pair_handler.py  | 验证括号自动配对         | 英文括号配对、中文括号配对、右括号跳过、设置开关                         | 输入 `(` 自动插入 `()`；输入 `（` 自动插入 `（）`；光标在配对内时右括号跳过而非插入；关闭设置后不配对                                   |
| test_config.py             | 验证配置管理器           | 初始化、编辑器设置、游戏设置、资源 CRUD、打字统计                        | 默认字体 12、自动配对 True；设置值持久化；资源默认 3000/1000；累加不超上限；打字统计跨日归零                                            |
| test_savegame_manager.py   | 验证存档管理器           | 加载/保存/加密状态/SavegameSaveResult/解锁数据恢复/加密文件备份/每日签到 | 加密/未加密存档加载；SKIPPED_ENCRYPTED_UNREAD跳过保存；解锁后重新加载原始数据；密码错误恢复加密状态；加密文件自动备份；每日签到奖励发放 |
| test_first_run_dialog.py   | 验证首次运行对话框       | 初始化、默认路径、确认创建目录                                           | 窗口标题"欢迎使用 PanzerNote"；路径编辑框显示默认值；确认后创建 config/notebooks 目录                                                   |
| test_game_sidebar.py       | 验证游戏侧边栏           | 按钮初始化、当前视图状态、视图切换信号                                   | 返回/建造/车厂/收藏按钮存在；`set_current_view` 高亮对应按钮；点击按钮发射 `view_changed` 信号                                          |
| test_highlight_themes.py   | 验证代码高亮主题         | 主题列表、获取主题、格式构建、预览 CSS、代码高亮 HTML                    | 返回可用主题列表；默认主题有样式；`build_format` 处理颜色/粗体/斜体；`highlight_code_html` 高亮 Python                                  |
| test_resource_bar.py       | 验证资源栏组件           | 资源项初始化、数值设置、颜色映射、刷新、打字统计                         | fuel 绿色/ammo 黄色/steel 蓝色/bauxite 橙色；`set_value` 更新显示；`refresh` 从 config 同步                                             |
| test_secretary_widget.py   | 验证小秘书组件           | 台词气泡、消息显示、事件台词、立绘格式化                                 | `SpeechBubble` 默认隐藏；`show_message` 显示文本；`show_event_message` 匹配事件台词；`format_line` 格式化                               |
| test_status_bar.py         | 验证状态栏组件           | 默认标签、统计更新、编码设置、文件类型设置                               | 默认"行 1, 列 1"/"0 个字符"/"UTF-8"/"纯文本"；`update_stats` 更新所有标签                                                               |
| test_syntax_highlighter.py | 验证语法高亮器           | Markdown 标题/代码块/行内代码/粗体/链接/列表/引用                        | H1-H3 正确匹配；代码围栏高亮；行内代码高亮；粗体/链接/列表/引用格式化                                                                   |
| test_dpi_helper.py         | 验证高 DPI 缩放工具      | 缩放因子、整数缩放、尺寸缩放、字体缩放、样式表缩放                       | `scale_factor` 在 1.0-3.0 之间；`scale(100)` ≥ 100；`scale_font(1)` 最小 8；`scale_stylesheet` 替换 px 值                               |
| test_error_handler.py      | 验证错误提示系统         | 路径过滤、堆栈过滤、IP 过滤、密码过滤、分类标签、建议映射                | Windows/Unix/Mac 路径替换为 `[已过滤]`；IP/密码/token 过滤；每个 ErrorCategory 有标签和建议                                             |
| test_shortcut_manager.py   | 验证快捷键管理器         | 初始化、默认加载、注册、回调、冲突检测、持久化                           | 默认快捷键加载；`register` 创建 QAction；触发调用回调；`check_conflicts` 检测冲突                                                       |
| test_path_validator.py     | 验证路径安全验证器       | 路径规范化、目录穿越检测、白名单、路径长度、控制字符                     | 相对路径转绝对；`../` 检测为穿越；白名单外路径拒绝；超长路径拒绝；控制字符拒绝                                                          |
| test_file_guard.py         | 验证文件操作安全         | 初始化、安全读写、字节读写、大小限制、超时控制                           | 默认 50MB/30s；`safe_write` + `safe_read` 往返一致；超限抛 `FileSizeExceededError`                                                      |
| test_crypto_manager.py     | 验证存档加密系统         | 加密解密、错误密码、损坏数据、中文数据、文件操作、迁移                   | 加密-解密往返一致；错误密码抛 `DecryptionError`；损坏数据抛异常；中文数据正确处理；迁移自动备份                                         |
| test_input_validator.py    | 验证输入验证框架         | 文件名验证、保留名、搜索注入、设置值验证、清洗接口                       | 合法文件名通过；`CON/PRN/AUX/NUL` 拒绝；特殊字符拒绝；搜索内容转义；设置值范围检查                                                      |
| test_plugin_system.py      | 验证插件基础架构         | 状态枚举、权限枚举、元数据 CRUD、基类生命周期、沙箱 API、权限检查        | 五种状态正确；八种权限值正确；`PluginMeta` 序列化/反序列化；`on_load/on_activate` 状态转换；沙箱超时/越权异常                           |
| test_plugin_manager.py     | 验证插件管理器           | 扫描、加载、激活、停用、卸载、热加载、清单验证、版本兼容                 | 空目录返回空列表；扫描发现插件；清单缺失跳过；版本不兼容拒绝；热加载重新加载                                                            |
| test_theme_system.py       | 验证主题系统             | 颜色方案、布局配置、主题定义、引擎加载、QSS 生成、预览对话框             | 默认颜色方案正确；`from_dict` 忽略未知字段；`ThemeEngine` 加载 JSON/YAML；QSS 包含所有 UI 元素选择器                                    |
| test_feature_flags.py      | 验证 Feature Flag 系统   | 已注册 flag 查询、未注册 flag 警告、默认值、配置持久化                   | 已注册 flag 返回正确值；未注册 flag 返回 False 并记录警告；`set_enabled` 不修改未注册 flag；配置文件加载/无效JSON容错                   |
| test_virtual_scroll.py     | 验证虚拟滚动管理器       | 大文件加载、异常回退、边界情况、模式切换                                 | 大文件返回 True 并激活虚拟滚动；`setPlainText` 异常回退普通模式；阈值边界；空内容/Unicode 处理                                          |

---

## 10. 开发注意事项

1. **编码保持**：打开文件时检测编码并记录在 `_tab_info["encoding"]`，保存时使用相同编码
2. **括号配对双路径**：英文走 `keyPressEvent`，中文走 `inputMethodEvent`，逻辑需同步维护（现已拆分到 `auto_pair_handler.py` Mixin）
3. **缩略图安全**：v1.5.4 修复了 `resizeEvent(None)` 导致卡死的 Bug，现使用 `_update_child_geometries()` 独立函数
4. **资源文件路径**：立绘/图标始终从 `_app_dir`（程序目录）读取，不随 `_base_path` 变化
5. **auto_minimap**：开启时 .txt/.md 不显示缩略图，关闭时使用全局 `show_minimap` 设置
6. **打字统计日期重置**：`get_today_chars_typed()` 自动比较 `today_date` 并在跨日时归零
7. **Feature Flag**：所有性能优化特性默认关闭，通过 `utils/feature_flags.py` 控制，配置持久化到 `feature_flags.json`
8. **Mixin 模式**：`Editor` 类通过 Mixin 继承组合功能（`EditorActionsMixin`、`AutoPairHandlerMixin`），mypy 对 Mixin 文件禁用了 `attr-defined`/`arg-type` 检查
9. **异步渲染线程安全**：`AsyncHighlightRenderer` 使用 `QueuedConnection` 信号通信，禁止在非主线程操作 UI 元素
10. **虚拟滚动**：大文件（≥50000行）自动启用延迟语法高亮，仅高亮可视区域及缓冲区
11. **高 DPI 缩放**：`main.py` 已启用 `AA_EnableHighDpiScaling`，因此 `dpi_helper.scale()` 系列函数在生产环境中为 no-op（直接返回输入值）。若未来关闭自动 DPI 缩放，则需通过 `dpi_helper.scale()` 系列函数获取缩放值
12. **快捷键管理**：`ShortcutManager` 已接入 `MenuBuilder`，所有菜单项通过 `manager.register()` 注册，自定义快捷键功能已生效
13. **错误提示**：使用 `ErrorHandler.show_error()` / `ErrorHandler.show_from_exception()` 替代直接 `QMessageBox`，确保敏感信息过滤和统一分类提示
14. **路径安全**：所有文件操作路径必须通过 `PathValidator` 白名单验证，禁止直接使用用户输入构造文件路径。`config.py` 在初始化时将 `_app_dir` 和 `_base_path` 加入白名单
15. **文件操作安全**：使用 `FileGuard.safe_read()` / `safe_write()` 替代直接 `open()`，确保文件大小和超时控制。内部配置文件读写可设置 `validate_path=False` 跳过白名单检查
16. **输入验证**：所有用户输入（文件名、搜索内容、设置值）必须通过 `InputValidator` 验证。文件名使用 `validate_filename_strict()`，搜索内容使用 `validate_search()`，设置值使用 `validate_setting()`
17. **存档加密**：`CryptoManager` 使用 PBKDF2+AES-GCM 加密存档数据，密钥由用户密码派生。迁移操作自动备份并验证数据一致性。加密状态通过 `is_encrypted()` 检测
18. **加密存档保存**：`SavegameManager.save()` 返回 `SavegameSaveResult` 枚举，加密但未解锁的存档返回 `SKIPPED_ENCRYPTED_UNREAD` 而非静默跳过。MainWindow 在关闭时检查此状态并提示用户输入密码解锁
19. **主题全局生效**：所有需要响应主题切换的 UI 组件应继承 `ThemeAwareMixin`，在 `_apply_theme(theme)` 中实现样式更新逻辑。`ThemeAwareMixin` 自动订阅 `theme_changed` 信号。当前已集成组件：`Editor`、`MarkdownPreviewWidget`、`EditorTabWidget`、`MinimapWidget`、`FileTreeWidget`、`FindReplaceBar`、`StatusBarWidget`、`SecretaryWidget`、`ResourceBar`、`GameSidebar`、`ShortcutPanel`
20. **每日签到**：`SavegameManager.check_daily_checkin()` 在每日首次启动时自动发放签到奖励（燃料/弹药/钢材/铝材各+100），通过 `MainWindow._check_daily_checkin()` 触发小秘书提示
21. **集中式版本管理**：`src/__init__.py` 中的 `__version__` 为唯一真相源，所有模块通过 `from src import __version__` 引用。`pyproject.toml` 使用动态版本配置，`scripts/verify_version.py` 提供一致性验证，`main.py` 启动时自动检查
22. **自动配对性能**（v1.6.6）：`AutoPairHandlerMixin` 使用 `frozenset` 缓存实现 O(1) 入口过滤，`_doc_char_at()` 替代 `toPlainText()` 全文复制，`_wrap_selection()` 替代 `selectedText()` 大字符串复制。修改 `AUTO_PAIR_CHARS` 字典后缓存自动失效重建
23. **粘贴检测**（v1.6.6）：`Editor` 重写 `insertFromMimeData()` 设置 `_is_pasting` 标志，`EditorTabWidget._on_text_changed()` 检查此标志跳过粘贴的打字奖励计数。`_PASTE_THRESHOLD = 50`，仅字符增量 ≤50 且非粘贴时计入奖励
24. **拖放文件白名单**（v1.6.6）：`MainWindow._SUPPORTED_DROP_EXTS` 定义允许拖入的文件扩展名，`dropEvent()` 对非白名单扩展名弹出警告。扩展白名单只需修改此 `frozenset`
25. **分屏语义**（v1.6.6）：`_split_editor()` 打开新空白文件而非当前文件副本，避免双标签编辑同一文件的数据覆盖风险。菜单文本标注"独立编辑"
26. **插件安全**（v1.6.6）：`PluginAPI._mvp_read_only` 为实例属性，不可通过类级别篡改；`register_command()` 使用 `dict.setdefault()` 原子操作，消除 TOCTOU 竞态条件
27. **MarkdownIt 实例复用**（v1.6.6）：`MarkdownPreviewWidget._create_md_parser()` 在 `__init__` 中创建一次解析器实例并缓存为 `_md_parser`，`_render_markdown()` 直接调用缓存实例渲染，消除每次按键重复实例化开销
28. **文件保存异步化**（v1.6.6）：`SaveTask`（`QRunnable`）+ `SaveTaskSignals`（`QObject`）将 `safe_write` 磁盘 IO 放到 `QThreadPool.globalInstance()` 后台线程执行。`_save_file()` 改为异步：主线程获取内容后提交后台任务，保存失败时回滚修改状态并恢复标签页 `*` 标记
29. **Markdown 预览增量更新**（v1.6.6）：`PREVIEW_HTML_TEMPLATE` 新增 `<div id="content">` 包裹。首次渲染走 `setHtml` 加载完整模板，`loadFinished` 信号触发后标记 `_html_template_loaded = True`，后续渲染通过 `runJavaScript` 仅更新 `innerHTML`。切换文档时自动重置标志。非 WebEngine 模式仍走全量路径
30. **Minimap 块级增量失效**（v1.6.6）：`MinimapWidget` 改用 `QTextDocument.contentsChange` 信号（带 `from_pos/chars_removed/chars_added` 参数），精确计算受影响缓存块范围并标记为脏块（`_block_dirty`），仅重新渲染脏块。行数变化时后续块也标记为脏。常规打字仅重绘 1 个块，节省约 95% 渲染开销
31. **封装规范**（v1.6.6）：`MainWindow._check_daily_checkin()` 中 `self.config._savegame_manager` 改为 `self.config.savegame_manager`，使用公开属性
32. **行尾符统一**（v1.6.6）：`editor.py`、`menu_builder.py`、`plugin_manager.py` 的 CRLF 行尾符统一转换为 LF，配合 `.gitattributes` 的 `* text eol=lf` 规则
33. **Silent except 日志补全**（v1.6.6）：所有 `except ... : pass` 模式改为 `except ... as e: logger.debug(...)` 或 `print(...)`（exceptions.py 避免循环依赖）。涉及文件：`minimap.py`、`markdown_preview.py`、`editor_tabs.py`、`config.py`、`exceptions.py`、`plugin_base.py`、`find_replace.py`、`plugin_manager.py`
34. **主题切换响应完善**（v1.6.6）：`MainWindow._apply_theme()` 新增 `_game_placeholder` 样式更新，确保主题切换时占位文本颜色同步。`FindReplaceBar._apply_theme_colors()` 末尾调用 `_update_match_label()` 确保匹配计数标签颜色随主题同步
35. **ErrorHandler 正则收紧**（v1.6.6）：`_SENSITIVE_PATTERNS` 中 `File "..."` 和 `line N` 两条独立正则合并为 `File "...", line N`，避免误过滤"on line 10""第 5 行"等普通文案
36. **导出安全**（v1.6.6）：`_export_pdf` 和 `_export_html` 中 `MarkdownIt` 均显式传入 `{"html": False}` 禁用 raw HTML；python-markdown fallback 仅启用 `tables` 扩展，不启用任何 HTML 相关扩展

---

_本文档基于 PanzerNote v1.6.6 源码生成，涵盖全部 Python 源文件和游戏数据 JSON。_
