PanzerNote v1.6.4 修改规划（基于综合审阅报告 v2）
第一部分：开始之前的详细提醒

在动工之前，请把以下提醒读完。这些是从审阅报告 v2 的 112 个问题中提炼出的"通用纪律"，跳过它们会导致返工或引入新问题。

1. 关于实施顺序

务必严格按阶段顺序：A → B → C → D → E。阶段间不得跳跃。原因：

阶段 A 修复"装不上"和"安全漏洞"，这些是任何后续工作的前提。pyproject.toml 错了连 pip install 都跑不通；Markdown 预览的 raw HTML 漏洞不修，调试时打开任意 .md 都有风险

阶段 B 让"看起来完成的"真正完成，避免后续 AI 协作时基于错误假设给建议

阶段 C 清理空架子。如果跳到阶段 D 直接做新功能，会在那些空架子上继续添砖加瓦——比如不修 ShortcutManager 就给图鉴系统加快捷键，最后图鉴的快捷键也无法自定义

阶段 D 才是核心玩法（建造、图鉴、车库），D 之前不要碰

阶段 E 是日常增强，按需做

2. 关于"虚假完成"陷阱

报告 v2 揭示了项目最大的系统性问题：多个子系统代码质量良好但完全未接入主流程——ShortcutManager、dpi_helper、LazyLoader、存储抽象层、IncrementalRenderer 的 _line_cache 等。

教训是：写完代码不等于功能落地，必须验证有业务调用方。今后修改时，每完成一个新模块，立即用 grep -rn "模块名\|关键类名" src/ 确认有非自身调用方，否则视为未完成。

3. 关于问题 ID 引用

报告中 112 个问题都有稳定 ID（如 BUG-013、SEC-001、ARCH-008）。与 AI 协作时直接引用 ID，例如说"帮我处理 BUG-013"，AI 能立刻定位到具体描述、影响、修复方案。不要用模糊描述如"那个快捷键问题"。

4. 关于"功能开发中"提示

阶段 A 的 BUG-004 修复要求把 9 个空函数改为弹"功能开发中"提示，不要直接实现这些功能。原因：

真实实现属于阶段 D 的工作，依赖架构整改

用户当前看到的"点击没反应"比"功能开发中"危害更大——前者像 bug，后者是清晰预期

留下提示反而能避免阶段 D 时漏掉某个入口

5. 关于先测试再改动

阶段 A 的 P0 修复每一项都涉及关键路径，改动前先确认对应单元测试存在。当前 29 个测试文件覆盖较好，但如果某项修改没有现成测试（比如 BUG-013 涉及 ShortcutManager 接入是没有测试的），先补一份冒烟测试再改代码。否则修复完无法验证。

6. 关于 feature flag 的使用

阶段 B 涉及性能改造（PERF-001、PERF-002、BUG-009 async_highlight 等），这些都必须挂在配置开关后。项目已有 feature_flags.py，新加 flag 时遵循该模块的命名（virtual_scroll、async_highlight 等下划线风格）。

默认值的选择规则：有任何不确定就默认关闭。例如 stats_timer 改为信号驱动（PERF-002），新引入的 flag 应叫 stats_signal_driven，默认 False，确认稳定后再切换为 True。

7. 关于"诚实化优先于真正实现"

报告中有几项标了"低难度（诚实化）/ 高难度（真正实现）"两种方案，例如：

ARCH-001 VirtualScrollManager：要么真做分块（高），要么改名 LazyHighlightManager（低）

ARCH-002 IncrementalRenderer：要么实现行级（高），要么改名 RenderCache（低）

SEC-004 插件沙箱：要么子进程隔离（高），要么改名"插件包装器"+ 文档说明（低）

全部选低难度方案——除非用户性能投诉，否则不要花时间做"真实现"。优雅的死代码不是优势，准确的命名才是。

8. 关于安全相关改动

阶段 A 的 SEC-001 ~ SEC-003 是真实的安全风险，不是过度警惕。修改时：

SEC-001 Markdown 关 raw HTML 后，必须测试一下原本含合法 HTML 的笔记是否仍能渲染表格、图片 —— 用户笔记中可能确实用过 HTML 表格

SEC-002 加 PathValidator 后，要测试正常的相对路径图片仍能加载（如 ![](./image.png) 应该正常工作）

SEC-003 加密存档保护守卫后，测试"用户启用加密 → 输错密码 → 程序保存设置"流程，确保存档不被覆写

9. 关于 ShortcutManager 接入（BUG-013）

BUG-013 是阶段 A 工作量最大的一项（半天到一天）。决策点：

方案 A（接入）：改 MenuBuilder 接受 ShortcutManager 实例，所有菜单项通过 manager.register(...) 然后 manager.get_action(action_id) 拿 QAction。真正让快捷键可定制。

方案 B（移除）：删掉 ShortcutPanel 的"自定义"功能，改成只读展示。

推荐方案 A——因为既然 600 行代码已经写了、测试已经写了、UI 已经做了，距离"真正能用"只差接入这一步。但方案 A 改动较大，单独开分支 fix/shortcut-manager-integration 做。

10. 关于数据安全（BUG-016）

阶段 B.2 的 BUG-016（file_tree 删除走回收站）虽然是 P1，但实质是用户最关心的"防止数据丢失"。强烈建议加 send2trash 库（约 200KB，纯 Python，零依赖）。如果不想加新依赖，至少要实现"移到 .trash 子目录"的降级方案，不要让用户的笔记直接物理删除。

11. 关于文档同步

每完成一个阶段，立即同步更新 项目说明.md 和 README.md。报告中的 DOC-001 ~ DOC-015 列了 15 项文档与代码不一致的事实，这种漂移就是来自"改了代码不改文档"。

具体规则：

删除模块时，文档对应章节也删除

改名（如 IncrementalRenderer → RenderCache）时，文档同步改名

修改 Python 版本要求、文件大小限制等关键参数时，README 与 pyproject.toml 必须一致

12. 关于 AI 协作的边界

报告本身有局限性，附录 §11.3 列出了未验证的部分（运行时行为、打包测试、跨平台行为）。这意味着：

关键改动（特别是阶段 A 的 P0）需要自己实际跑一下程序验证，不能光看 AI 改完的 diff 就合并

Windows 特有问题（如 \\?\ 长路径前缀、MAX_PATH_LENGTH）必须在 Windows 实机测试

pyinstaller 打包测试只能你自己做，AI 帮不了

13. 关于报告本身的更新

修改过程中如果发现报告描述与代码不符，优先以代码为准，并在报告对应 ID 处加注释。比如某项已经修复了，就在那条问题前加 [已修复 - YYYY-MM-DD] 标记，不要直接删除——保留历史便于追溯。PanzerNote v1.6.4 修改规划（基于综合审阅报告 v2）
第一部分：开始之前的详细提醒

在动工之前，请把以下提醒读完。这些是从审阅报告 v2 的 112 个问题中提炼出的"通用纪律"，跳过它们会导致返工或引入新问题。

1. 关于实施顺序

务必严格按阶段顺序：A → B → C → D → E。阶段间不得跳跃。原因：

阶段 A 修复"装不上"和"安全漏洞"，这些是任何后续工作的前提。pyproject.toml 错了连 pip install 都跑不通；Markdown 预览的 raw HTML 漏洞不修，调试时打开任意 .md 都有风险

阶段 B 让"看起来完成的"真正完成，避免后续 AI 协作时基于错误假设给建议

阶段 C 清理空架子。如果跳到阶段 D 直接做新功能，会在那些空架子上继续添砖加瓦——比如不修 ShortcutManager 就给图鉴系统加快捷键，最后图鉴的快捷键也无法自定义

阶段 D 才是核心玩法（建造、图鉴、车库），D 之前不要碰

阶段 E 是日常增强，按需做

2. 关于"虚假完成"陷阱

报告 v2 揭示了项目最大的系统性问题：多个子系统代码质量良好但完全未接入主流程——ShortcutManager、dpi_helper、LazyLoader、存储抽象层、IncrementalRenderer 的 _line_cache 等。

教训是：写完代码不等于功能落地，必须验证有业务调用方。今后修改时，每完成一个新模块，立即用 grep -rn "模块名\|关键类名" src/ 确认有非自身调用方，否则视为未完成。

3. 关于问题 ID 引用

报告中 112 个问题都有稳定 ID（如 BUG-013、SEC-001、ARCH-008）。与 AI 协作时直接引用 ID，例如说"帮我处理 BUG-013"，AI 能立刻定位到具体描述、影响、修复方案。不要用模糊描述如"那个快捷键问题"。

4. 关于"功能开发中"提示

阶段 A 的 BUG-004 修复要求把 9 个空函数改为弹"功能开发中"提示，不要直接实现这些功能。原因：

真实实现属于阶段 D 的工作，依赖架构整改

用户当前看到的"点击没反应"比"功能开发中"危害更大——前者像 bug，后者是清晰预期

留下提示反而能避免阶段 D 时漏掉某个入口

5. 关于先测试再改动

阶段 A 的 P0 修复每一项都涉及关键路径，改动前先确认对应单元测试存在。当前 29 个测试文件覆盖较好，但如果某项修改没有现成测试（比如 BUG-013 涉及 ShortcutManager 接入是没有测试的），先补一份冒烟测试再改代码。否则修复完无法验证。

6. 关于 feature flag 的使用

阶段 B 涉及性能改造（PERF-001、PERF-002、BUG-009 async_highlight 等），这些都必须挂在配置开关后。项目已有 feature_flags.py，新加 flag 时遵循该模块的命名（virtual_scroll、async_highlight 等下划线风格）。

默认值的选择规则：有任何不确定就默认关闭。例如 stats_timer 改为信号驱动（PERF-002），新引入的 flag 应叫 stats_signal_driven，默认 False，确认稳定后再切换为 True。

7. 关于"诚实化优先于真正实现"

报告中有几项标了"低难度（诚实化）/ 高难度（真正实现）"两种方案，例如：

ARCH-001 VirtualScrollManager：要么真做分块（高），要么改名 LazyHighlightManager（低）

ARCH-002 IncrementalRenderer：要么实现行级（高），要么改名 RenderCache（低）

SEC-004 插件沙箱：要么子进程隔离（高），要么改名"插件包装器"+ 文档说明（低）

全部选低难度方案——除非用户性能投诉，否则不要花时间做"真实现"。优雅的死代码不是优势，准确的命名才是。

8. 关于安全相关改动

阶段 A 的 SEC-001 ~ SEC-003 是真实的安全风险，不是过度警惕。修改时：

SEC-001 Markdown 关 raw HTML 后，必须测试一下原本含合法 HTML 的笔记是否仍能渲染表格、图片 —— 用户笔记中可能确实用过 HTML 表格

SEC-002 加 PathValidator 后，要测试正常的相对路径图片仍能加载（如 ![](./image.png) 应该正常工作）

SEC-003 加密存档保护守卫后，测试"用户启用加密 → 输错密码 → 程序保存设置"流程，确保存档不被覆写

9. 关于 ShortcutManager 接入（BUG-013）

BUG-013 是阶段 A 工作量最大的一项（半天到一天）。决策点：

方案 A（接入）：改 MenuBuilder 接受 ShortcutManager 实例，所有菜单项通过 manager.register(...) 然后 manager.get_action(action_id) 拿 QAction。真正让快捷键可定制。

方案 B（移除）：删掉 ShortcutPanel 的"自定义"功能，改成只读展示。

推荐方案 A——因为既然 600 行代码已经写了、测试已经写了、UI 已经做了，距离"真正能用"只差接入这一步。但方案 A 改动较大，单独开分支 fix/shortcut-manager-integration 做。

10. 关于数据安全（BUG-016）

阶段 B.2 的 BUG-016（file_tree 删除走回收站）虽然是 P1，但实质是用户最关心的"防止数据丢失"。强烈建议加 send2trash 库（约 200KB，纯 Python，零依赖）。如果不想加新依赖，至少要实现"移到 .trash 子目录"的降级方案，不要让用户的笔记直接物理删除。

11. 关于文档同步

每完成一个阶段，立即同步更新 项目说明.md 和 README.md。报告中的 DOC-001 ~ DOC-015 列了 15 项文档与代码不一致的事实，这种漂移就是来自"改了代码不改文档"。

具体规则：

删除模块时，文档对应章节也删除

改名（如 IncrementalRenderer → RenderCache）时，文档同步改名

修改 Python 版本要求、文件大小限制等关键参数时，README 与 pyproject.toml 必须一致

12. 关于 AI 协作的边界

报告本身有局限性，附录 §11.3 列出了未验证的部分（运行时行为、打包测试、跨平台行为）。这意味着：

关键改动（特别是阶段 A 的 P0）需要自己实际跑一下程序验证，不能光看 AI 改完的 diff 就合并

Windows 特有问题（如 \\?\ 长路径前缀、MAX_PATH_LENGTH）必须在 Windows 实机测试

pyinstaller 打包测试只能你自己做，AI 帮不了

13. 关于报告本身的更新

修改过程中如果发现报告描述与代码不符，优先以代码为准，并在报告对应 ID 处加注释。比如某项已经修复了，就在那条问题前加 [已修复 - YYYY-MM-DD] 标记，不要直接删除——保留历史便于追溯。

整体推进建议

分支策略：每个阶段独立分支，命名建议：

fix/p0-emergency（阶段 A）

fix/p1-quality（阶段 B）

refactor/p2-arch（阶段 C）

feat/p1-todo（阶段 D）

feat/enhancement（阶段 E）

节奏建议：

阶段 A 单日完成，立即合并主干

阶段 B 分子阶段（B.1 ~ B.6）每完成一个就合并

阶段 C 的 1-9 项小改动可合并为一个 PR，10-14 项每项一个独立 PR

阶段 D 每个子系统（D.1/D.2/D.3/D.4）独立 PR

阶段 E 每批一个 PR

回归保障：阶段 A、B 的每个 PR 必须运行完整 pytest。阶段 C、D 涉及架构改动，PR 中需说明影响范围 + 手动测试清单。

整个 4-6 周计划完成后，项目应当具备：清晰的模块边界、稳健的安全防护、完整的核心玩法、文档与代码完全自洽。届时可以考虑发布 v2.0 作为里程碑。

如需将本规划拆分为 GitHub Issue 模板、转换为甘特图，或就某个 ID 给出具体 diff，告诉我即可继续。