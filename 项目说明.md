# PanzerNote v1.6.1 — 项目架构与设计文档

> 本文档旨在让任何人（包括新对话中的 AI）快速全面了解本项目的**目标、当前实现、代码结构、数据流、以及未完成规划**。

---

## 1. 项目背景

PanzerNote 是一款以已停服二次元游戏《战车少女》（PanzerMaiden）为主题的 **PC 端离线单机记事本程序**，用于私人缅怀。核心理念：

- **记事本为主体**：功能完整的多标签文本编辑器，支持 Markdown 分屏预览与 30+ 语言语法高亮
- **游戏系统为灵魂**：通过日常书写行为积累资源 → 资源投入建造 → 抽取战车娘角色 → 点亮图鉴
- **小巧、离线、本地存档**：所有数据存储在本地 JSON 文件中，不依赖网络

**技术栈**：Python 3.8+ / PyQt5 / Pygments / markdown 库 / QWebEngine（可选）

---

## 2. 当前完成状态总览

| 模块 | 状态 | 说明 |
|------|------|------|
| 多标签文本编辑器 | ✅ 完成 | 含行号、语法高亮、自动缩进、括号配对、行操作、大小写转换、转到行、JSON/XML格式化 |
| Markdown 分屏预览 | ✅ 完成 | 实时渲染 + 代码块语法高亮 + 一键复制 + 本地图片支持 |
| 代码缩略图 (Minimap) | ✅ 完成 | 右侧鸟瞰图，彩色像素块，点击/拖拽导航，auto_minimap 支持 |
| 增强型查找替换 | ✅ 完成 | 正则、大小写敏感、全词匹配、匹配计数、全部高亮 |
| 文件树 | ✅ 完成 | 左侧文件树 + 外部文件区 + 右键菜单（新建/删除/重命名）+ 接受标签拖拽移动文件 |
| 标签页拖拽 | ✅ 完成 | 标签内排序 + 拖拽到文件树移动文件 |
| 资源栏 | ✅ 完成 | 顶部显示四资源 + 打字统计（UI 层面） |
| 在线/离线挂机 | ✅ 完成 | 在线每分钟 +5/+5/+5/铝材每3分钟+5；离线 1/3 向大取整，上限24h |
| 小秘书 | ✅ 完成 | 右下角立绘 + 台词气泡 + 点击互动 + 事件台词系统 + 自定义角色/皮肤/状态 |
| 设置系统 | ✅ 完成 | settings.json + workspace.json + savegame.json，首次运行对话框选择路径 |
| **打字奖励** | 🔲 规划中 | 打字字符数 → 资源奖励（已有数据结构 `today_chars_typed`，未接入 `textChanged`） |
| **建造系统** | 🔲 规划中 | 四资源配方 + 等待时间 + 阈值过滤 + 概率抽取（已有 characters.json 数据） |
| **图鉴系统** | 🔲 规划中 | 已获得/未获得 + 立绘查看 + 语音播放（已有 savegame 中 `owned_characters`） |
| **车库系统** | 🔲 规划中 | 角色详情界面 |
| **游戏设置界面** | 🔲 规划中 | 占位 `_show_game_settings` 空函数 |

---

## 3. 目录结构

```
PanzerNote/
├── main.py                         # 程序入口（高DPI、字体、图标、首次运行引导、创建主窗口）
├── requirements.txt                # PyQt5, Pygments, markdown, Pillow
├── user_data_path.txt              # 持久化用户数据路径（方案A）
│
├── src/                            # ══════ 源代码 ══════
│   ├── __init__.py
│   ├── config.py                   # [496行] 配置管理中枢（settings/workspace/savegame 三文件读写）
│   ├── main_window.py              # [1099行] 主窗口（菜单栏 + 布局组装 + 定时器 + 挂机逻辑 + 事件路由）
│   ├── editor.py                   # [1152行] 核心编辑器（行号、缩略图、语法高亮、自动缩进、括号配对、行操作、大小写、格式化）
│   ├── editor_tabs.py              # [1096行] 多标签管理（打开/保存/关闭/编码检测/另存为编码/DraggableTabBar/文件移动）
│   ├── syntax_highlighter.py       # [273行] 语法高亮（Pygments适配器 + Markdown专用高亮器 + 工厂函数）
│   ├── highlight_themes.py         # [325行] 主题系统（PyCharm Light 配色，编辑器 QTextCharFormat + 预览 CSS + 内联HTML）
│   ├── markdown_preview.py         # [727行] Markdown分屏（PreviewBrowser浮动复制按钮 + 代码块高亮 + 本地图片 + 滚动同步）
│   ├── minimap.py                  # [238行] 代码缩略图（逐字符彩色像素块 + 视口指示器 + 缓存 + 鼠标导航）
│   ├── find_replace.py             # [471行] 查找替换栏（正则/大小写/全词 + ExtraSelections高亮 + 替换/全部替换）
│   ├── editor_settings_dialog.py   # [157行] 记事本设置对话框（行号/高亮/缩略图/字体/行宽/自动保存/括号配对）
│   ├── file_tree.py                # [410行] 文件树（QFileSystemModel + DroppableTreeView + 外部文件标签）
│   ├── resource_bar.py             # [150行] 资源栏UI（四色图标 + 千分位数值 + 打字统计标签）
│   ├── game_sidebar.py             # [177行] 游戏侧边栏（返回/建造/车库/图鉴 四个占位按钮）
│   ├── secretary_widget.py         # [381行] 小秘书（立绘加载 + 台词气泡 + 事件台词 + 角色切换 + 位置跟随）
│   ├── status_bar.py               # [112行] 状态栏（行列位置 + 字符数 + 编码 + 文件类型）
│   └── first_run_dialog.py         # [174行] 首次运行（选择数据存储路径 + 创建目录结构 + 复制资源）
│
├── data/                           # ══════ 数据 & 资源 ══════
│   ├── assets/
│   │   ├── icons/
│   │   │   └── app_icon.png        # 应用图标
│   │   ├── portraits/              # 角色立绘（见下方架构说明）
│   │   │   ├── secretary.png       # 默认小秘书立绘
│   │   │   ├── 原始/正常/          # {编号} {角色名}-正常.png
│   │   │   ├── 原始/大破/          # {编号} {角色名}-大破.png
│   │   │   ├── 皮肤/正常/          # {编号} {角色名} {皮肤名}-正常.png
│   │   │   └── 皮肤/大破/          # {编号} {角色名} {皮肤名}-大破.png
│   │   └── voices/                 # 语音文件（预留）
│   ├── config/                     # 配置文件目录（用户数据路径下）
│   │   ├── settings.json           # 编辑器/游戏/小秘书/视图/窗口设置
│   │   └── workspace.json          # 会话状态（打开的文件列表、最近文件、外部文件）
│   └── gamedata/
│       ├── characters.json         # 角色数据库（10个示例角色，含阈值/建造时间/稀有度）
│       ├── secretary_lines.json    # 小秘书台词配置
│       └── savegame.json           # 游戏存档（资源、核心、打字统计、建造队列、已拥有角色）
│
├── notebooks/                      # 用户笔记库（默认含 工作/回忆/日记 三个子文件夹）
└── temp/autosave/                  # 自动保存暂存区（退出时清理）
```

---

## 4. 核心模块详解

### 4.1 配置管理 (`config.py`)

Config 类是整个应用的配置中枢，管理三个 JSON 文件：

| 文件 | 位置 | 职责 |
|------|------|------|
| `settings.json` | `{base_path}/data/config/` | 所有用户偏好设置（编辑器、游戏、小秘书、视图、窗口） |
| `workspace.json` | `{base_path}/data/config/` | 会话状态（打开的文件列表、最近文件、外部文件、文件树展开状态） |
| `savegame.json` | `{base_path}/data/gamedata/` | 游戏存档（四资源、核心数、打字统计、建造队列、拥有角色、成就） |

**路径机制（方案A）**：
- 程序目录下保存 `user_data_path.txt`，记住用户选择的数据存储路径
- 首次运行由 `FirstRunDialog` 引导选择
- 资源文件（立绘、图标等）始终从**程序目录** `_app_dir` 读取
- 用户数据（笔记、配置、存档）从 `_base_path` 读取

**关键方法**：
- `_merge_dict(default, current)` — 递归合并，确保新增配置项向下兼容
- `get_resources()` / `add_resource()` — 资源 CRUD
- `get_today_chars_typed()` — 自动按日期重置计数器
- `update_last_login()` — 离线收益的时间基准

### 4.2 主窗口 (`main_window.py`)

**布局结构**（从上到下、从左到右）：
```
┌──────────────────────────────────────────────────────┐
│  ResourceBar（资源栏：燃料/弹药/钢材/铝材 + 统计）    │
├──────────────────────────────────────────────────────┤
│  GameSidebar │ QSplitter                             │
│  (50px固定)  │ ┌─────────┬─────────────────────────┐ │
│  ← 返回      │ │FileTree │ FindReplaceBar (隐藏)   │ │
│  建 建造     │ │(可折叠)  │ EditorTabWidget          │ │
│  库 车库     │ │          │  ┌───┬───┬───┐           │ │
│  鉴 图鉴     │ │          │  │Tab│Tab│Tab│           │ │
│              │ │          │  ├───┴───┴───┤           │ │
│              │ │          │  │ Editor /   │           │ │
│              │ │          │  │ MD Preview │           │ │
│              │ │          │  │        ┌──┐│           │ │
│              │ │          │  │   小秘书│  ││           │ │
│              │ └─────────┴──┴────────┴──┘│           │ │
├──────────────┴──────────────────────────────────────┤
│  StatusBar（行/列 | 字符数 | 编码 | 文件类型）        │
└──────────────────────────────────────────────────────┘
```

**定时器**：
- `auto_save_timer`（默认30秒）→ 调用 `save_all_to_temp()`
- `stats_timer`（500ms）→ 更新状态栏和资源栏统计
- `idle_reward_timer`（60秒）→ 在线挂机资源发放

**挂机机制**：
- **在线**：每分钟 fuel/ammo/steel +5，bauxite 每3分钟+5（用 `bauxite_counter` 计数）
- **离线**：启动时 `_calculate_offline_rewards()` 根据 `last_login` 计算时间差，收益 = 在线的 1/3（向大取整），上限24h，最少5分钟

### 4.3 编辑器 (`editor.py`)

基于 `QPlainTextEdit`，是整个记事本功能的核心。主要功能块：

| 功能区 | 说明 |
|--------|------|
| **行号** | `LineNumberArea` 子控件，重写 `paintEvent` 绘制行号，动态计算宽度 |
| **缩略图** | 内嵌 `MinimapWidget`，通过 `_update_child_geometries()` 管理几何位置 |
| **语法高亮** | 调用 `get_highlighter_for_file()` 工厂函数，支持 Pygments 和内置 Markdown |
| **自动缩进** | `_handle_enter()` 保持缩进 + 检测 `:`/`{` 等触发额外缩进 |
| **括号配对** | `keyPressEvent` + `inputMethodEvent` 双路径处理英文和中文 IME 输入 |
| **行操作** | 删除行 / 复制行 / 上下移动行，通过 `beginEditBlock/endEditBlock` 保证 undo 原子性 |
| **大小写** | `toggle_case` / `to_uppercase` / `to_lowercase` / `to_titlecase` |
| **格式化** | JSON (`json.dumps`) 和 XML (`minidom.toprettyxml`) 格式化 |
| **右键菜单** | 全中文化菜单，包含大小写子菜单和格式化选项 |

**括号配对的复杂性**：
- 支持英文 `() [] {} "" ''` 和中文 `（）【】「」『』《》〈〉""''`
- `keyPressEvent` 处理英文键盘直接输入
- `inputMethodEvent` 处理中文 IME 提交的字符（中文标点走这个路径）
- 智能判断：左右均有非空白字符且不是已有配对 → 不配对
- 嵌套输入：已有配对符号之间输入新配对符号 → 正常补全
- 引号智能：通过 `_pick_single_cjk_quote()` 用未闭合引号栈判断应插入左/右引号
- Backspace 成对删除
- 选中文本时自动包裹

### 4.4 标签页管理 (`editor_tabs.py`)

| 组件 | 说明 |
|------|------|
| `DraggableTabBar` | 继承 `QTabBar`，标签内拖拽 = 重排序，拖出标签栏 = 发起 `QDrag`（携带文件路径 MIME） |
| `EditorTabWidget` | 继承 `QTabWidget`，管理 `_tab_info` 字典（filepath/modified/encoding/is_markdown 等） |
| `SaveAsDialog` | 自定义另存为对话框，支持编码选择（UTF-8/GBK/UTF-16） |

**核心逻辑**：
- `open_file()` — 编码级联检测（UTF-8 → GBK → UTF-16 → 容错UTF-8），Markdown 文件自动使用 `MarkdownPreviewWidget`
- `_on_text_changed()` — 比较当前内容与 `last_saved_content`，决定是否标记为已修改（标签名加 ` *`）
- `move_file_to_folder()` — 先保存最新内容 → `shutil.move` → 更新 `_tab_info` 中的 filepath
- 所有编辑操作（undo/redo/cut/copy/paste/行操作/大小写/格式化）通过代理方法转发给当前编辑器

### 4.5 Markdown 预览 (`markdown_preview.py`)

**渲染管线**：
```
编辑器文本 → markdown 库渲染 HTML → _process_code_blocks()（Pygments内联样式高亮 + 浅蓝容器 + Unicode标记）
           → _resolve_local_images()（相对路径 → file:// 绝对路径）
           → PREVIEW_HTML_TEMPLATE 包裹 → QTextBrowser/QWebEngineView 显示
```

**浮动复制按钮** (`PreviewBrowser`)：
- 在每个代码块 HTML 首尾嵌入不可见 Unicode 标记（⌜N⌝ / ⌞N⌟）
- `setHtml` 后用 `QTextDocument.find()` 缓存标记的 QTextCursor
- `mouseMoveEvent` 判断鼠标是否在某代码块垂直范围内，是则显示浮动复制按钮
- 防抖 30ms + 按钮 enter/leave 处理防止闪烁

### 4.6 主题系统 (`highlight_themes.py`)

当前仅实现 `pycharm_light` 主题，但架构支持扩展：
- `THEMES` 字典：每个主题 = `{Token: {color, bold, italic, underline, background}}`
- 编辑器端：`get_editor_formats()` → `{Token: QTextCharFormat}`
- 预览端：`highlight_code_html()` → Pygments HtmlFormatter + 内联 style
- 新增主题只需在 `THEMES` 中增加条目 + `settings.json` 中 `editor.code_highlight_theme` 切换

### 4.7 小秘书 (`secretary_widget.py`)

- 固定在 `editor_container` 右下角（通过 `eventFilter` 监听父容器 `Resize` 事件）
- 台词系统：`secretary_lines.json` 定义多种事件（启动/保存/建造/闲置/点击等），支持 `{nickname}` 和 `{self}` 占位符
- 立绘加载：按 `character_id/character_name/skin_name/state` 构建路径，fallback 到 `secretary.png`

---

## 5. 数据文件详解

### 5.1 `characters.json` — 角色数据库

当前包含 10 个示例角色。每个角色结构：
```json
{
  "id": "char_001",
  "name": "虎王",
  "name_en": "Tiger II",
  "type": "heavy",           // light/medium/heavy/super_heavy/td/spg
  "nation": "G",             // G=德/S=苏/U=美/E=英/J=日/F=法/C=中/I=意
  "rarity": 5,               // 1普通~6神话
  "build_time_minutes": 300,
  "threshold": {
    "total": 1800,            // 四资源总投入最低门槛
    "fuel": 500,              // 各项最低门槛（可选）
    "steel": 800,
    "bauxite": 200            // 某些特殊角色需要铝材门槛
  },
  "portrait": "...",
  "voice_intro": "..."
}
```

同时定义了 `types`（6种车型）、`nations`（8国）、`rarities`（6级稀有度 + 颜色）。

### 5.2 `savegame.json` — 游戏存档

```json
{
  "resources": { "fuel": 3000, "ammo": 3000, "steel": 3000, "bauxite": 1000 },
  "cores": 0,
  "last_login": "2025-01-01T12:00:00",
  "today_date": "2025-01-01",
  "today_chars_typed": 0,
  "total_chars_typed": 0,
  "total_documents": 0,
  "construction_queue": [],     // 规划：建造队列
  "owned_characters": {},       // 规划：已拥有角色 {id: {count, first_obtained, ...}}
  "achievements": []            // 规划：成就系统
}
```

### 5.3 `settings.json` — 用户设置

分五个命名空间：`editor`（编辑器）、`game`（游戏）、`secretary`（小秘书）、`view`（视图）、`window`（窗口几何）。所有设置都有 `DEFAULT_SETTINGS` 兜底默认值。

---

## 6. 信号/事件流

### 6.1 文件打开流

```
用户双击文件树 / Ctrl+O
  → FileTreeWidget.file_open_requested(filepath)
  → MainWindow._open_file(filepath)
     → 判断是否笔记库外文件 → config.add_external_file()
     → EditorTabWidget.open_file(filepath)
        → 编码级联检测
        → 判断是否 Markdown → 创建 Editor 或 MarkdownPreviewWidget
        → 生成 tab_id，存入 _tab_info
        → set_file_type() → 绑定语法高亮 + auto_minimap
     → config.add_recent_file()
```

### 6.2 在线挂机资源发放

```
idle_reward_timer (每60秒)
  → MainWindow._on_idle_reward()
     → config.add_resource("fuel", 5)
     → config.add_resource("ammo", 5)
     → config.add_resource("steel", 5)
     → bauxite_counter++ → 满3时 add_resource("bauxite", 5)
     → resource_bar.refresh()
```

### 6.3 标签拖拽移动文件

```
DraggableTabBar.mouseMoveEvent (鼠标离开标签栏)
  → QDrag(MIME_TAB_FILEPATH = filepath UTF-8)
  → DroppableTreeView.dropEvent
     → 解析 MIME → 确定目标文件夹
     → FileTreeWidget.file_move_requested(src, dest)
     → MainWindow._on_file_move_from_tree(src, dest)
        → EditorTabWidget.move_file_to_folder(src, dest)
           → 保存最新内容 → shutil.move → 更新 _tab_info
        → secretary.show_message("已移动...")
```

---

## 7. 远期规划（未实现部分详述）

以下是开发者在项目开始前的原始设计意图，当前代码中已有部分数据结构预留但功能未实现。

### 7.1 打字奖励系统

**设计**：在编辑器的 `textChanged` 中接入有效击键统计，过滤粘贴/撤销等非手动输入，仅计入实际键入字符。

**规则**：
- 每日前 1000 字 100% 计入；1000–3000 字 40%；更高 10%（递减收益）
- 每日上限 10,000 字符
- 累计达到阈值（默认500字符）触发一次资源奖励（燃料/弹药/钢材各+10，铝材+3）

**代码现状**：`config.py` 已有 `today_chars_typed` / `total_chars_typed` / `daily_typing_limit` 等字段，`main_window.py` 的 `_update_stats()` 已读取 `today_chars_typed`，但 `textChanged` 信号**未接入**字符计数逻辑。

### 7.2 建造系统

**设计**：
1. **输入**：用户输入四项资源配方 → 扣除资源 → 进入建造队列（默认2个槽位）
2. **等待时间**：与投入总量相关，可用加速券/核心跳过
3. **阈值过滤**：先做硬阈值过滤（总投入、单项最低要求），不够则该角色绝不出现
4. **概率抽取**：
   - 总投入越高 → 稀有池权重越高（有上限/平滑函数）
   - 配方比例影响倾向：Steel高→重型，Ammo高→火力型，Fuel高→机动型，Bauxite高→特殊型

**代码现状**：`characters.json` 已定义了10个角色（含 threshold 和 build_time_minutes），`savegame.json` 已有 `construction_queue` 和 `construction_slots` 字段，`game_sidebar.py` 已有"建造"按钮但视图切换后显示空白容器。

### 7.3 图鉴系统

**设计**：
- 未获得角色灰色/占位，获得后点亮
- 可查看立绘截图、播放语音（mp3）
- 重复获得转化为"核心/碎片"，可用于加速建造/扩容槽位/提高挂机效率

**代码现状**：`savegame.json` 有 `owned_characters` 和 `cores` 字段，`characters.json` 有完整的角色元数据和稀有度定义。

### 7.4 车库系统

**设计**：角色详情界面，查看已拥有角色的立绘、属性、获得记录。

**代码现状**：仅有侧边栏"库"按钮和空白 `game_view_container`。

---

## 8. 快捷键总览

| 分类 | 快捷键 | 功能 |
|------|--------|------|
| 文件 | `Ctrl+N/O/S/Shift+S/W` | 新建/打开/保存/另存为/关闭标签 |
| 编辑 | `Ctrl+Z/Y/X/C/V/A` | 撤销/重做/剪切/复制/粘贴/全选 |
| 查找 | `Ctrl+F/H/G`, `F3/Shift+F3` | 查3/Shift+F3` | 查找/替换/转到行/下一个/上一个 |
| 行操作 | `Ctrl+Shift+K`, `Alt+↑↓`, `Ctrl+Shift+D` | 删除行/移动行/复制行 |
| 大小写 | `Ctrl+Shift+U` | 切换大小写 |
| 视图 | `Ctrl+B/M/Shift+P`, `F11`, `Ctrl+±0` | 文件树/缩略图/MD预览/全屏/缩放 |
| 导航 | `Ctrl+1/2/3/4` | 记事本/建造/车库/图鉴 |

---

## 9. 依赖与运行

```bash
pip install PyQt5>=5.15 Pygments>=2.15 markdown>=3.4.0 Pillow>=9.0.0
pip install PyQtWebEngine  # 可选，Markdown预览用QWebEngineView（否则退化为QTextBrowser）
python main.py
```

---

## 10. 开发注意事项

1. **编码保持**：打开文件时检测编码并记录在 `_tab_info["encoding"]`，保存时使用相同编码
2. **括号配对双路径**：英文走 `keyPressEvent`，中文走 `inputMethodEvent`，逻辑需同步维护
3. **缩略图安全**：v1.5.4 修复了 `resizeEvent(None)` 导致卡死的 Bug，现使用 `_update_child_geometries()` 独立函数
4. **资源文件路径**：立绘/图标始终从 `_app_dir`（程序目录）读取，不随 `_base_path` 变化
5. **auto_minimap**：开启时 .txt/.md 不显示缩略图，关闭时使用全局 `show_minimap` 设置
6. **打字统计日期重置**：`get_today_chars_typed()` 自动比较 `today_date` 并在跨日时归零

---

*本文档基于 PanzerNote v1.6.1 源码生成，涵盖全部 17 个 Python 源文件（总计约 7500 行代码）和 2 个游戏数据 JSON。*

